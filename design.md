# Taskport 設計書

> タスクとスケジュールを、どの端末からでも・どんな方法でも入れられて、
> 必要な形で出せるようにするための個人用ハブ。

- **リポジトリ名**: `taskport`
- **公開URL**: `https://tcta-tottori.github.io/Taskport/`
- **バージョン**: v1.0（初版）
- **作成日**: 2026-08-18

---

## 1. 背景と目的

### 1.1 現状の課題

生産管理部の業務では、タスクが発生する瞬間と、それを記録できる瞬間がずれる。

- 現場や倉庫を歩いている最中に「あれを確認しないと」と気づくが、その場でPCは開けない
- 会議・電話の直後に3件も4件も宿題が積まれるが、メモを取る前に次の用件が来る
- メールやチャットで来た依頼が、受信箱に埋もれてタスク化されない
- 結果として、期限と優先順位が頭の中にしか存在しない状態になる

### 1.2 このアプリが解決すること

**入口を増やして、記録のコストをゼロに近づける。**
歩きながらなら喋る。PCの前ならキーボード。メールから来た依頼なら共有ボタン。
入口が何であれ、行き先は一つの場所にまとまる。

**出口を増やして、記録が死蔵在庫にならないようにする。**
一覧で見る、日付軸で見る、カレンダーに送る、日報用に書き出す。
入れた情報が、使いたい形で取り出せる。

### 1.3 名前の由来

**port = 入出力ポート**。多数の入口と出口を持ち、そのすべてが同じ一つの場所につながっている、という構造をそのまま名前にした。「Voice」を名前に入れないのは意図的で、音声は数ある入口の一つに過ぎない。

---

## 2. スコープ

### 2.1 やること

| 区分 | 内容 |
|---|---|
| 入力 | 音声・キーボード・フォーム直接・他アプリからの共有 |
| 構造化 | 自然文をAIがタスク候補に分解し、**人が確認・修正してから**登録 |
| 管理 | 期限・優先度・区分・メモ・完了状態 |
| 出力 | 一覧／スケジュール／カレンダー連携／テキスト書き出し／CSV |
| 動作環境 | Android（主）・PC（副）。PWAとしてホーム画面に追加 |

### 2.2 やらないこと（初版）

- チーム機能（担当者アサイン、コメント）→ Phase 3 で検討
- ガントチャート、工数管理、進捗率
- 社内基幹システムとの直接連携
- ネイティブアプリ化（ストア配信）

### 2.3 前提

- 主な利用者は1名（Phase 1〜2）
- 利用ブラウザは **Chrome**（Android / Windows）。音声入力は Chrome 前提とし、非対応環境ではキーボード入力に自動フォールバック
- 業務情報（取引先名・品番・数量）を扱うため、セキュリティ要件は §8 に従う

---

## 3. 設計思想

### 3.1 中核となる比喩

```
        入口（Ports In）                            出口（Ports Out）
   ┌──────────────────┐                      ┌──────────────────┐
   │ 音声              │                      │ 一覧ビュー        │
   │ キーボード（自然文）│                      │ スケジュールビュー │
   │ フォーム（直接）   │  ──▶  [ Taskport ] ──▶ │ Googleカレンダー  │
   │ 他アプリから共有   │        単一の台帳      │ テキスト書き出し   │
   │ カレンダー取込     │                      │ CSV / スプレッドシート│
   └──────────────────┘                      └──────────────────┘
```

**設計上の含意**: 入口と出口は将来必ず増える。したがってコードは、
入口を「自然文 → タスク候補」の共通パイプラインに集約し、
出口を「タスク配列 → 任意の形式」の変換器として独立させる。
入口を1つ足すのに本体を触らずに済む構造にすること。

### 3.2 三つの原則

**原則1 — AIは提案し、人が決める**
自然文の解釈は必ず確認画面を挟む。全自動登録はしない。誤った期限が黙って入る方が、確認の一手間より遥かに高くつく。

**原則2 — 音声は端末から出さない**
音声認識はブラウザ内蔵の Web Speech API で端末側処理する。外部に送るのは認識後のテキストのみ。取引先名や数量を含む音声データそのものをクラウドに置かない。

**原則3 — 保存層は差し替え可能にしておく**
Phase 1 は端末内、Phase 3 は共有ストレージ。ここを最初からインターフェースで分離しておけば、共有化の際に画面側を書き直さずに済む。

---

## 4. アーキテクチャ

### 4.1 構成図

```
┌──────────────────────────────────────────────────┐
│  ブラウザ（Android Chrome / PC Chrome）             │
│                                                  │
│   音声 ──▶ Web Speech API ──▶ テキスト             │
│   録音 ──▶ MediaRecorder  ──▶ 音声（端末内のみ）    │
│                    │                             │
│              ┌─────▼──────┐                      │
│              │ parseToTasks│ 端末内で解析          │
│              └─────┬──────┘ 外部へ送らない         │
│                    ▼                             │
│              ┌───────────┐                       │
│              │ ReviewSheet│ 必ず人が確認           │
│              └─────┬─────┘                       │
│  ┌──────────┐      ▼                             │
│  │  UI層     │ ┌────────────┐  ┌──────────────┐   │
│  │  React    │─│ Repository │─▶│  IndexedDB   │   │
│  └──────────┘ │ （抽象）    │  │ taskport      │   │
│               └────────────┘  │ taskport-media│   │
│                               └──────────────┘   │
└───────────────────────┬──────────────────────────┘
                        │ 利用者が選んで押したときだけ
                        ▼
              ┌──────────────────────┐
              │ Google Calendar API   │  予定の読込・追加
              └──────────────────────┘
```

### 4.2 技術選定

| 層 | 採用 | 理由 |
|---|---|---|
| ビルド | Vite | 軽量・高速。GitHub Pages と相性が良い |
| UI | React + TypeScript | 型でタスク構造を固定でき、入出力追加時の事故を防ぐ |
| PWA | vite-plugin-pwa | オフライン動作とホーム画面追加を標準機能で |
| 保存 | IndexedDB（`idb` ラッパ） | 容量制限が緩く、オフラインで確実に残る |
| 音声 | Web Speech API | 無料・端末内処理・Android Chrome で日本語精度が高い |
| 解析 | 端末内の自前ロジック | 外部サービスを使わない（§10.1）。文章が端末から出ない |
| ホスティング | GitHub Pages | 無料・`onshitsu` と同じ運用で学習コストゼロ |
| カレンダー | Google Identity Services + Calendar API | 予定の読込と追加。クライアントIDは利用者が用意する |

### 4.3 ディレクトリ構成

```
taskport/
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── types.ts                  # Task / Draft / 列挙型の定義
│   ├── ports/
│   │   ├── in/
│   │   │   ├── useVoiceInput.ts      # 音声 → テキスト
│   │   │   ├── useTextInput.ts       # 自然文メモ → テキスト
│   │   │   ├── useShareTarget.ts     # 他アプリからの共有受け取り
│   │   │   └── parseToTasks.ts       # テキスト → タスク候補（共通パイプライン）
│   │   └── out/
│   │       ├── toCalendar.ts         # Googleカレンダー用URL / ICS生成
│   │       ├── toPlainText.ts        # 日報・朝会用テキスト
│   │       └── toCsv.ts              # CSV書き出し
│   ├── repository/
│   │   ├── Repository.ts         # インターフェース定義
│   │   ├── LocalRepository.ts    # Phase 1: IndexedDB
│   │   └── SheetRepository.ts    # Phase 3: GAS + スプレッドシート
│   ├── views/
│   │   ├── ListView.tsx
│   │   ├── ScheduleView.tsx
│   │   ├── ReviewSheet.tsx       # AI候補の確認・修正
│   │   ├── TaskEditor.tsx
│   │   └── ExportSheet.tsx
│   ├── components/
│   │   ├── TaskCard.tsx
│   │   ├── InputDock.tsx         # 画面下部の入力ドック
│   │   └── Segmented.tsx
│   └── styles/
│       └── tokens.css            # 色・タイポのトークン
├── public/
│   ├── manifest.webmanifest
│   └── icons/
├── design.md
├── CLAUDE.md
└── README.md
```

---

## 5. データモデル

### 5.1 Task

```ts
type Priority = "high" | "mid" | "low";
type Status   = "open" | "done";
type Source   = "voice" | "text" | "form" | "share" | "calendar";

interface Task {
  id: string;            // ULID（時系列にソート可能なID）
  title: string;         // 「〜する」で終わる実行形
  note: string;          // 相手先・数量・背景などの補足
  due: string | null;    // "YYYY-MM-DD"。期限なしは null
  dueTime: string | null;// "HH:mm"。時刻指定がある予定のみ
  estimateMin: number | null; // 見込み所要時間（分）。未見積は null
  startedAt: string | null;   // 実際に手を付けた時刻（ISO）。未完了で入っていれば実行中
  actualMin: number | null;   // 実際にかかった時間（分）。見込みとは別に持つ
  priority: Priority;
  categories: string[];  // 区分。複数可。先頭が主区分（v1.11.0 で category: string から変更）
  status: Status;
  source: Source;        // どの入口から入ったか（改善のため記録）
  createdAt: string;     // ISO 8601
  updatedAt: string;
  doneAt: string | null;
}
```

**設計上の注記**

- `priority` は内部的に英字。表示は 高／中／低。将来の多言語化と、CSVでの並べ替えを考慮
- `categories` は複数持てる（1つの作業が発注と納期確認の両方にまたがる）。候補は業務日報の作業内容から起こしたマスタで、**利用者が画面から編集できる**（グループ・色・語の追加や削除）。マスタは `Settings.categoryGroups` が持ち、既定値は `src/lib/workCategories.ts`。マスタ外の語も入れられて、その場合は「その他」として集計する。
  **先頭が主区分**で、時間の集計（分析の「区分ごとの時間」）と日報の書き出しは先頭だけを見る。件数の集計は付いている区分すべてで数える
- `source` を持つのは、「どの入口が実際に使われているか」を後から見て、育てる入口を判断するため
- 削除は物理削除とする。個人用途で論理削除の複雑さに見合う利点がない
- `estimateMin` は勤務時間に対する積み上げ量の計算とスケジュールの帯の長さに使う。
  null（未見積）は Settings の `defaultEstimateMin` で埋めて計算する（§10 参照）
- `startedAt` / `actualMin` は**実績**（§6.2.2c）。見込みと同じ欄に入れないのは、
  片方を直したときにもう片方の意味が変わるため。日報と「区分ごとの時間」は
  実績があれば実績を、無ければ見込みを使い、**どちらで埋めたかを画面に書く**

### 5.1b Job（案件＝工数の単位）

```ts
interface Job {
  id: string          // ULID
  name: string        // 「AB-1234 立ち上げ」
  code: string        // 管理番号・品番。空でよい
  client: string      // 相手先（得意先・部署）。空でよい
  plannedMin: number  // 見積工数（分）。0 は「決めていない」
  due: string | null  // "YYYY-MM-DD"
  closed: boolean     // 締めた案件は一覧の下へ畳む（実績は残す）
  note: string
  createdAt: string
  updatedAt: string
}
```

タスクと予定が `jobId` で案件を指す。区分（`categories`）が**何をしたか**なら、
案件は**何のためにしたか**。同じ「伝票処理」でも、A社の立ち上げぶんと定常ぶんを
分けて数えたい、が実際に起きる。

**工数の数え方は稼働と同じ**で、押して測った時間だけを実績にする（`lib/jobs.ts`）。

| 出す数字 | どこから | 注意 |
|---|---|---|
| 実績（合計） | タスクは台帳の `actualMin`、予定は実行ログ | 押して測ったぶんだけ |
| 期間（今週・今月） | 実行ログの区間 | ログは 90日ぶんしか残らない |
| 残り見込み | 未完了タスクの `estimateMin` | 実績とは別の欄。混ぜない |
| 見積 | 案件の `plannedMin` | 人が入れた予算。実績とは別の列 |

案件は**台帳とも予定とも別のDB**（`taskport-jobs`）に置く。
保存先が増えるたびに既存のDBの版を上げない、という決まり（§10.1）に従う。

### 5.2 Draft（AI候補・未保存）

```ts
interface Draft extends Omit<Task, "id"|"status"|"createdAt"|"updatedAt"|"doneAt"> {
  tempId: string;
}
```

確認画面でのみ存在し、登録時に `Task` へ変換される。

### 5.3 Repository インターフェース

```ts
interface Repository {
  list(): Promise<Task[]>;
  add(tasks: Task[]): Promise<void>;
  update(id: string, patch: Partial<Task>): Promise<void>;
  remove(id: string): Promise<void>;
}
```

画面側は **必ずこのインターフェース経由**でデータに触る。
`LocalRepository` と `SheetRepository` を差し替えるだけで保存先が変わること。

設定もタスクと同じ層に置く。保存先を差し替えたときに設定だけ端末に取り残されないため。

```ts
interface Repository {
  // ...上記に加えて
  loadSettings(): Promise<Settings>;
  saveSettings(settings: Settings): Promise<void>;
  replaceAll(tasks: Task[]): Promise<void>;  // JSONバックアップの取り込み用
}
```

### 5.4 Settings（勤務時間）

```ts
interface WorkHours {
  start: string;       // 始業 "HH:mm"
  breakStart: string;  // 昼休憩の開始
  breakEnd: string;    // 昼休憩の終了
  end: string;         // 終業 "HH:mm"
  workDays: number[];  // 稼働曜日。0=日 〜 6=土
}

interface Settings {
  workHours: WorkHours;
  defaultEstimateMin: number;  // 未見積タスクを積むときの既定値（分）
  voiceEnabled: boolean;
  whisperModel: string;        // 端末内Whisperのモデル（既定 base）
  geminiEnabled: boolean;      // 文章の解析にGeminiを使うか（既定 false）
  geminiModel: string;         // Geminiのモデル
}
```

**GeminiのAPIキーは Settings に入れない。** 設定は端末どうしの同期に乗るので、
キーは端末内（localStorage）にだけ置く（`src/lib/gemini.ts`）。

```ts
```

**既定値**（利用者の実際の勤務に合わせる。設定画面から変更可能）

| 項目 | 値 |
|---|---|
| 始業 | **8:20** |
| 昼休憩 | **12:25 〜 13:05** |
| 終業 | **17:10** |
| 実働 | 4時間05分 + 4時間05分 = **8時間10分**（490分） |
| 稼働曜日 | 月〜金 |

この値は表示だけでなく計算に使う。

- スケジュールビューの目盛り（始業・昼休憩・終業の線）
- その日のタスク見込み合計 ÷ 実働490分 = **稼働率**
- 一覧トップと分析画面の稼働ゲージ
- 勤務時間そのものの ICS 書き出し

---

## 6. 機能仕様

### 6.1 入口（Ports In）

#### 6.1.1 音声入力

録音まわりは NoteLoop 9.2（アプリ版）と同じ仕組みにそろえる。違うのは出口だけで、
NoteLoop が議事録を作るところを、Taskport はタスク候補を作る。

- 右下の **＋ → マイク**（扇の左端。長押しのまま滑らせても選べる）で開始。
  録音中はポップアップに切り替わる
- 上にウェーブと経過時間、その下に認識テキストをそのまま流す
- `continuous: true` / `interimResults: true` / `lang: "ja-JP"`。
  認識が勝手に切れたら確定分をコミットして張り直す
- 終了時に自動で AI 構造化へ渡し、**確認画面（ReviewSheet）を必ず通す**

**表示**は NoteLoop 9.2 の体裁にそろえる。並びは
経過時間 → ゲージ → 「文字起こし」の仕切り → 認識テキスト → 案内文 →
左:やめる／中央:停止／右:一時停止。

ゲージは固定位置の縦バーが音量で伸縮する形（本ごとに伸びやすさと
揺れの速さを変えてあり、静かになると丸い点に戻る）。

ただし**全画面ではなく画面下半分のポップアップ**として出す。
上半分に一覧が見えたまま録音できる。背景は暗くするだけで、
タップしても録音は止めない（うっかり触れて消えるのを防ぐ）。

**画面を消しても止まらないようにする**（NoteLoop と同じ3点）

| 仕組み | 目的 |
|---|---|
| 常駐通知（Service Worker） | 録音中の表示。通知から一時停止・録音完了ができる |
| ほぼ無音の音声をループ再生 | メディア再生中のタブは凍結されないため、画面オフでも録音が続く |
| Wake Lock（任意） | 画面を点けたままにする。設定で切り替え |

**録音の取りこぼし対策**（NoteLoop と同じ3段構え）

- 3秒ごとにデータを取り出し、常に手元に確定させる
- 4秒ごとに録音が生きているか見張り、止まっていたらマイクを取り直して継ぎ足す
- 停止時に「経過時間」と「音声の実長」を比べ、足りなければ画面で知らせる

**あとから高精度で取り直す（v1.15.0）**

録音中に出るのは Web Speech の文字で、速いかわりに取りこぼす。
そこで、**保存してある音声そのもの**をあとから端末内の Whisper に通して、
精度の高い文字に置き換えられるようにした（NoteLoop 9.2 と同じ組み立て）。

| いつ | 何を使うか | 待つか | 音声はどこへ |
|---|---|---|---|
| 録音中 | Web Speech API（その場に出る） | 待たない | ブラウザ経由でクラウドへ（認識のみ） |
| 停止後（押したときだけ） | 端末内 Whisper（`public/whisper-worker.js`。既定） | 待つ | どこへも出ない |
| 停止後（押したときだけ） | Gemini（APIキーを入れたときだけ出る） | 待つ | **音声そのものがGoogleへ** |

- 置き場は**確認画面**と**録音履歴**の2か所。**押したときだけ**走らせる
  （スマホの CPU では1分の録音で数十秒〜数分かかる。毎回待たせない）
- 取り直したら、その文字で候補を作り直し、録音履歴の文字も差し替える
- モデルは設定で選ぶ（tiny / base / small / large-v3-turbo）。既定は **base（約80MB）**
- 処理は Worker の中で完結する。**音声は端末から出ない。**
  外へ取りに行くのは仕組み（transformers.js）とモデルだけで、初回のみ

**音声の保存**

録った音声は端末内（IndexedDB）にのみ置く。外部へ送るのは認識後のテキストだけ。
新しい方から20本まで残し、古いものは自動で消す。録音履歴から再生・保存・削除ができ、
「あのタスク、本当にそう言ったか」を後から確かめられる。設定で保存を切ることもできる。

エラー時の挙動:

- `not-allowed` → マイク許可の確認を促し、キーボード入力へ誘導
- `no-speech` → 認識は張り直し、録音は続ける
- API 未対応 → ボタンをキーボード入力に切り替え

#### 6.1.2 キーボード入力（自然文）

音声と**まったく同じ**構造化パイプラインを通す。PCでの主入力手段。
メールの文面をそのまま貼り付けてタスク化する用途も想定する。

v1.11.0 で**独立した入口をやめ、「タスクを作る」画面（6.1.3）の中の
「文章から作る」に入れた**。下部に3つ並んでいたボタンのうち、どれが自然文で
どれが1件入力なのかが利用者に伝わらなかったため（§10.1）。通る道は変えていない。

#### 6.1.3 ＋（始める／作る）と7つの入口

画面**右下の ＋** を押す（または長押しする）と、＋の周りの 1/4 円に沿って
7つの入口が開く。**2層**（v1.22.0）で、内側の層が「その日に手を動かすこと」、
外側の層が「あとでやることを作ること」。

**1層目（＋に近い側・半径 100px）** — いま動かす／その日に入れる

| 位置 | 入口 | 開くもの |
|---|---|---|
| 真上（90°） | 区分 | 「区分から始める」。1件立ててその場で数え始める |
| 130° | 始める | 「タスクを始める」。台帳から選んで数え始める |
| 176° | 予定 | 予定を入れる画面（打合せ・固定の業務。今日が下敷き） |

**2層目（外側・半径 172px）** — あとでやることを作る

| 位置 | 入口 | 開くもの |
|---|---|---|
| 真上（90°） | 手描き | 空のフォーム（自分で書いて1件作る） |
| 118° | 記憶 | 記憶したタスクの一覧。選ぶと中身の入ったフォームが開く |
| 146° | 文章 | 自然文の画面（6.1.2）。まとめて候補にする |
| 174° | マイク | 録音を始める（6.1.1） |

**名前はアイコンの真下**（v1.23.0。利用者の指示）。丸は 46px（狭い端末は 44px）で、
半径は 100 / 172px（狭い端末は 98 / 168px）。
1層目の始める系（区分・始める）は実行と同じ緑、予定は菫、
2層目の作る系はブランドの藍で描き分ける。

角度と半径は「名前が隣の丸に重ならない」ことから決めてある。
名前を下に出すぶん、**同じ層の隣とは縦に、層どうしは半径で**間を取る必要があり、
実測（412px と 320px の端末幅で全部の丸と名前の当たりを見る）で詰めた。

**どれもそれぞれ独立した画面**（v1.13.0）。扇で選んだものがそのまま開く。
「区分」と「始める」も別々の画面で開く（v1.22.0。同じ画面のタブに入れない）。

**長押しはそのまま滑らせて選べる。** 指を置いたまま扇が開き、目当てのアイコンまで
滑らせて離すとそこが動く（押す→離す→また押す の3手が1手で済む）。
どのアイコンにも乗せずに離したときは開いたままにして待つ。
外を押すか、＋（開いている間は ✕）を押すと閉じる。

フォーム（手描き）は AIを経由せず1件を直接作る経路で、件名と期限だけ
分かっている時に最短で入れる。

決める操作は ✓、やめる操作は ✕ の丸ボタンだけにしてある。

#### 6.1.4 他アプリからの共有（Web Share Target）

`manifest.webmanifest` に `share_target` を宣言し、Android の共有メニューに Taskport を出す。
Gmail・LINE・ブラウザから文面を共有すると、その本文が構造化パイプラインに入る。

```json
"share_target": {
  "action": "/taskport/share",
  "method": "GET",
  "params": { "title": "title", "text": "text", "url": "url" }
}
```

**これが「いろんな方法で入力」の中核**。メールで来た依頼をタスク化する経路が、
アプリを開き直さずに成立する。

#### 6.1.5 AI構造化パイプライン（共通）

すべての自然文入力は `parseToTasks(text)` に集約される。
読み手は2つあり、**どちらを通っても出口は `Draft[]` で、必ず確認画面に出る**。

| 読み手 | いつ通るか | 文章はどこへ |
|---|---|---|
| 端末内（`localParse`） | 既定 | どこへも出ない |
| Gemini | APIキーを入れ、設定「文章の解析にGeminiを使う」を入れたときだけ | Googleへ出る |

Gemini が失敗したときは端末内で読み直し、**どちらで読んだかを確認画面に出す**
（どこを通ったか分からないまま登録させない）。

- 入力: 自然文、今日の日付
- 出力: `Draft[]`
- 挙動:
  - 複数の用件を含む文は複数タスクに分割
  - 「明日」「来週月曜」「今月末」を実日付に変換
  - 期限の言及がなければ `due = null`
  - 「至急」「今日中」→ high、数日以内の期限あり → mid、それ以外 → low
  - 相手先・数量・背景は `note` へ

**必ず確認画面（ReviewSheet）を経由し、無確認で登録しない。**

### 6.2 出口（Ports Out）

#### 6.2.1 一覧ビュー

タブ: **今日 / 今週 / すべて / 完了**

- 今日 = 期限が今日以前の未完了（超過分を含む）
- 今週 = 期限が7日以内の未完了
- 並び順: 期限昇順 → 優先度降順
- 期限超過は日数を赤字で表示（「3日超過」）
- 各カードの左端に優先度の色帯
- 手順（サブタスク）を持つカードは「2/4」と進み具合の帯を出し、開くとその場でチェックできる

**検索と絞り込み**（タブの上）

- 件名・メモ・区分・手順を横断。空白区切りは AND、NFKC で全角半角と大文字小文字の揺れを吸収
- 区分（大分類）／優先度／期限（超過・今日・7日以内・それ以降・期限なし）／完了を含めるか
- 絞り込み中はタブを離れ、台帳の全件から探す
- よく使う条件は名前を付けて残せる（端末内にのみ保存）

#### 6.2.1b 繰り返しと手順

- **繰り返し** … 毎日／稼働日ごと／毎週（曜日を選ぶ）／毎月おなじ日／毎月末。
  完了にした時点で次の1件だけを作る（§10.1）。「いつまで」を過ぎたら作らない。
  自然文からも拾う（「毎週月曜に…」「毎営業日に…」「毎月末に…」）。
  **期限は無くてもよい**（v1.14.0）。定常業務には締切という感じのものが無く、
  欄を埋めるためだけに日付を入れると、黙って超過扱いになる。期限が無いときは
  **完了にした日を起点**に次回の日を決め、次回ぶんにその日付が入る
- **手順（サブタスク）** … 1階層。全部済にしてもタスクは完了にならない。
  繰り返しの次回ぶんには、チェックを外した状態で引き継ぐ

#### 6.2.1c 期限のリマインド

時刻を入れたタスクの N 分前（既定10分）に通知する。期限が日付だけのものは対象外。

- 予約つき通知が使える端末 … 7日先までを Service Worker に預ける。閉じていても出る
- 使えない端末 … アプリを開いている間だけタイマーで出す

どちらも取りこぼしがあるため、いまどちらで動いているかと出ない条件を設定画面に書く。
既定は切。プッシュサーバは持たないので、予定が外部へ出る経路はない。

#### 6.2.2 スケジュールビュー

上に選んだ1日、下に2週間ぶんの日付軸。「いつ何が固まっているか」を面で把握するための出口。
期限なしタスクは末尾に「期限未設定」として集約。

**1日の見せ方は、勤務時間の枠ごとに畳んだ表**（v1.16.0）。
枠は午前前半／午前後半／午後前半／午後後半／時間外で、日報の時間区分と同じもの。

| 出すもの | 内容 |
|---|---|
| 枠の見出し | 名前・時間帯・積み上げ / 枠の長さ・帯 |
| 枠の中身 | その枠に入る仕事（時刻があれば「8:30–10:00」、無ければ見込みの長さ） |
| 枠のあいだ | 昼休憩・小休憩を細い行で |
| 枠の外 | 終日の予定、**枠を決めていない仕事** |

初版は1分＝0.9px の目盛りに置くタイムラインだったが、やめた（§10.1）。
実際の1日は予定の入っていない時間のほうが長く、画面のほとんどが余白になり、
**時刻を決めていない仕事が下へ押し出されて見えなくなっていた**。

#### 6.2.2b 分析ビュー

処理の傾向を面で見るための出口。グラフの作りは同じ作者の StudyDeck（成績画面）に合わせる。

並びは**設定が持ち、画面から並べ替えられる**（v1.23.0）。既定は次の順。

| # | カード | 内容 |
|---|---|---|
| 1 | 本日の稼働 | 勤務時間に対する積み上がり（%）＋連続処理日数＋未完了/超過/今週/完了。**数えるのは「始める／終わる」で実際に測った時間だけ**（v1.22.0） |
| 2 | 時間帯 | その日の勤務時間を帯にして、何時に何をしていたかを区分の色で置く（`DayBand`）。**同時に動かしたぶんは行を分ける**。押すとポップアップ（`BandSheet`）が開き、一覧の形で読んで実績を直せる |
| 3 | その日の区分の割合 | 円グラフ（ドーナツ）。実際に測った時間だけで出す。区分を押すとその仕事の実績を直せる |
| 4 | 区分ごとの時間 | 大分類ごとの時間と構成比。日報の集計と同じ見方。**完了したタスクの見込み時間の集計**であり実績ではない旨をカードに明記する |
| 5 | 区分ごとの進み具合 | 区分別の完了率を横バーで |
| 6 | 処理の推移 | 日別の完了件数（棒）と消化率（折れ線）を1つの図に。表示日数はスライダー |
| 7 | 未完了の優先度分布 | 高/中/低の残件数を縦バーで |
| 8 | どの入口から入ったか | `source` 別の内訳。育てる入口を判断するために置く |

「並べ替え」を押すと各カードの頭に ↑↓ が出て、押した順に入れ替わる。
並びは `Settings.dashOrder`（カードの鍵の配列）に入り、
出す前に `lib/dashCards.ts` の `normalizeDashOrder` を通す
（カードが増えても、保存された並びで落ちない）。

#### 6.2.2c 実績（いまやっている業務・やった業務）

一覧とスケジュールが「これから」を見る出口なのに対し、**手が動いた記録**を
日ごとに見る出口。日報はここから書き出す。

| 置き場 | 内容 |
|---|---|
| いま動いているもの | 手を付けているものと、始めてからの経過。押した時刻を残すだけで、タイマーは持たない（画面を閉じても止まらない） |
| その日の記録 | 完了したもの・手を付けたもの・その日が期限のもの。開始時刻とかかった時間をその場で直せる |
| やったことを足す | 会議・電話・応援のように、台帳に無いまま終わった仕事を後から1件入れる。件名・区分・開始時刻・かかった時間だけを聞く |

- **始める／止める** は一覧のカードと「今日の進めかた」からも押せる。
  完了にした時点で、始めてから完了までを実績として残す（すでに実績が入っていれば触らない）
- その日に出すかどうかは**完了した日**で見る。期限だけで見ていた頃は、
  昨日ぶんを今朝片づけた1件がどちらの日の日報にも出てこなかった
- 実績が入っていない仕事は見込みの時間で並ぶ。**そのことを画面に書く**
  （実績と見込みが混ざったまま「実績」と言わない）

#### 6.2.3 Googleカレンダー

**書き出し（認証不要）**

- 単発: カレンダー登録URLを生成して開く
- 一括: 選択したタスクを `.ics` として書き出し、カレンダーにインポート
- 時刻なしのタスクは終日予定として扱う
- 勤務時間（始業・昼休憩・終業）そのものを ICS にできる

**連携（要認証）**

利用者が自分の Google Cloud で OAuth クライアントIDを作り、設定画面に入れて使う。
アプリにIDを埋め込まない。

| 向き | 内容 |
|---|---|
| 読込 | 2週間分の予定を読み、スケジュールの帯の右側に破線で重ねる。台帳には混ぜない |
| 取込 | 予定をタップするとタスク候補になる。**確認画面を通してから**登録する |
| 追加 | 選んだタスクを予定として追加する。押したときに選んだ分だけを送る |

トークンはメモリにだけ置き、localStorage には保存しない。

#### 6.2.4 テキスト書き出し

日報・朝会用に整形したプレーンテキストをクリップボードへ。

```
【8/18（火）実施】
・サンプル商事に AB-1234 の注残8000本の前倒し可否を確認 → 完了
・パレット稟議書を仕上げる

【翌日以降】
・8/20 サンプル物流 田中様へ CD-5678 の納期回答
```

#### 6.2.4b 業務日報の書き出し

資材課の業務日報（エクセル）へ貼り付けるための表。勤務時間を 30 分で刻んだ
16 コマに、その日のタスクを時刻順に置く。区切りはタブ（区分名に読点が入るため
CSV では割れる）。30 分を超えるタスクはコマ数ぶんに広げ、時刻の無いタスクは
空いているコマへ順に置く。

```
8:20～8:50	納期確認、日程調整	サンプル商事へ AB-1234 の納期を確認する
8:50～9:20	発注業務、所要量確認	CD-5678 を200個発注する
```

#### 6.2.5 CSV書き出し

3つ出せる。Excelでの集計や、上長への共有の逃げ道として必ず用意する。

| 形 | 中身 |
|---|---|
| 台帳 | タスクの全項目（見込み・実績・区分・案件・状態） |
| 工数（作業ごと） | **実績から**「どの作業に何時間かかったか」。件名×区分×案件でまとめ、時間（0.1時間刻み）・分・回数・日数。期間は 日／週／月／全体（v1.30.0） |
| 工数（案件ごと） | 案件の 実績／見積／残り見込み／進捗%。実績は押して測った時間だけ |

工数の数え方は分析の画面とまったく同じ（`lib/analysis.ts`）。
画面と書き出しで数字が違うと、突き合わせができない。

### 6.3 画面一覧

**画面は5つ**（v1.30.0 で工数の画面をやめ、案件ごとの時間は分析の面へ入れた）。
一覧とカレンダーの画面もやめ、
一覧は実行へ、カレンダーはスケジュールの最後の面へ集約した（v1.24.0）。
**見出しはカタカナ、無ければ漢字**（v1.26.0 で英語から戻した）。
例外はスケジュールの3つの面だけで、**DAY / WEEK / MONTH** の英語1語にしてある（v1.27.0）。

| 画面 | 役割 |
|---|---|
| 実行（WorkLogView） | 起動時の既定画面。上から 朝の仕分け／明日の準備 → 実行中（動いていなければ面ごと出さない）→ 停止中 → 次の作業 → 近日締切 → **タスク（台帳。検索・絞り込み・4つのタブ）**。v1.28.0 で「やったことを足す」「本日の記録」「区分から開始」を外し、**いま手を動かすもの**だけにした。v1.32.0 で「よくやる業務」も右下の ＋ へ移した |
| スケジュール（ScheduleView） | DAY（**縦軸の時間の並び**。予定・タスクに加えて **✓ の付いた実績**（押して動かした記録）も同じ軸に置く（v1.30.0）＋ 時刻未定＝時刻を決めていない未完了（**空きへ置ける**）＋ やったことを足す。左上に面の名前・右上に日付。日付帯は **◀▶ で1週間ずつどこまでも前後へずらせる**（v1.32.0））→ 期限超過 → WEEK（**予定のある日だけ**）→ MONTH（月の升目） |
| 分析（DashboardView） | **いちばん上に期間の切り替え**（日／週／月／全体）と前後の送り。どの面も同じ期間で描く。並びは 区分の割合 → 時間帯（**縦軸**。週以上は時刻ごとの合計）→ 区分ごとの時間 → 推移 → 稼働 → 作業ごとの工数 → 案件ごとの工数 → 見込みと実績 → 曜日ごと → 区分別の進捗 → 優先度 → 入口別。並びは設定が持ち、画面から入れ替えられる。実績を直すのは円グラフと帯から（`ActualSheet`） |
| 録音 | 録音履歴。再生・音声の保存・削除 |
| 設定 | 勤務時間・会社カレンダー・同期・リマインド・区分のマスタ・書き出し／取り込み |

面（シート）は画面ではなく、どの画面からも同じものが開く。

| シート | 役割 |
|---|---|
| ReviewSheet | AI候補の確認・修正・件単位の破棄 |
| TaskEditor | 手描き（1件を作る）／既存タスクの編集 |
| TextSheet | 文章から作る（自然文をまとめて候補にする） |
| CategorySheet | 区分をグループの階層から複数選ぶ。マスタ（グループ・色・語）の編集も |
| TemplateSheet | 記憶したタスクの呼び出し |
| PlanSheet | 予定（打合せ・固定の業務）を作る・直す。自動で計上するかもここで切り替える |
| StartSheet | 「区分から始める」「タスクを始める」（それぞれ別に開く） |
| BandSheet | 時間帯（縦軸）と一覧。行を開くとその場で実績を直せる |
| JobSheet | 案件（工数の単位）を作る・直す。名前・管理番号・相手先・見積工数・期限。分析の「案件ごとの工数」から開く |
| ActualSheet | 実績（開始時刻・かかった時間）を直す |
| RecordingOverlay | 録音中のポップアップ（画面下半分）。経過時間・ゲージ・認識テキスト |
| ExportSheet | 出力形式の選択 |

---

## 7. デザイン方針

### 7.1 トークン

初版は白地の伝票調で設計していたが、NoteLoop 9.2 に合わせてダークテーマへ変更した（§10.1）。
実装値は `src/styles/tokens.css` を正とする。

```css
:root{
  /* 面と文字 — マットなグレーがかった黒。層は明度差ではなく落ち影で作る */
  --bg:      #17181b;
  --surface: #212328;
  --ink:     #e7e9ee;
  --muted:   #a3a9b5;
  --line:    #2e3138;
  /* ブランド（入口＝Ports In の色） */
  --brand1:  #7b93ff;
  --brand2:  #a68cff;
  --on-brand:#17181b;  /* 明るいブランド面の上に載せる墨 */
  /* 優先度 — 初版の色相を保ち、暗い面で読める明度に上げた */
  --pri-high:#ff7a6b;  /* 高（元 #B03A28） */
  --pri-mid: #e2a33f;  /* 中（元 #B87A22） */
  --pri-low: #78aed6;  /* 低（元 #3F7194） */
  --go:      #4ade80;  /* 完了・確定操作 */
}
```

### 7.2 タイポグラフィ

- 本文: システムの日本語ゴシック（`-apple-system` → `Hiragino Kaku Gothic ProN` → `Noto Sans JP` → `Yu Gothic UI`）。Webフォントは読み込まない（通信環境の悪い倉庫内で確実に表示させるため）
- **日付・優先度・件数は等幅（`ui-monospace`）＋ `tabular-nums`**。桁が揃うことで一覧の走査が速くなる。生産管理の伝票の質感を意図している
- 小ラベルは 10px・`letter-spacing: .14em` の大文字。装飾ではなく、情報の種別を示す標識として使う

### 7.3 レイアウト原則

- **入力ドックは常に画面下部固定**。片手持ちの親指到達域に録音ボタンを置く
- カードの角丸は 3px に抑える。丸すぎるとタスクが「柔らかいもの」に見え、期限の緊張感が消える
- 優先度は色帯（左端5px）で示し、テキストに色を付けすぎない

### 7.4 品質の下限（明示的に満たすこと）

- 320px 幅まで崩れない
- 文字と背景のコントラストは WCAG AA（本文 4.5:1）以上
- キーボードのみで全操作が可能、フォーカスリングを消さない
- `prefers-reduced-motion: reduce` でパルスアニメーションを停止
- タップ領域は最小 44×44px

---

## 8. セキュリティとデータの扱い

業務情報（取引先名・品番・数量・社内の人名）を扱う前提で、以下を厳守する。

| 項目 | 方針 |
|---|---|
| 音声データ | 端末内で処理し、外部送信しない |
| 自然文の解析 | 端末内で完結。外部のAIサービスへは送らない |
| タスクデータ | 端末内 IndexedDB のみ。**例外は Googleカレンダーへの追加だけ**で、利用者が選んで押したときにその分のみ送る（§10.1） |
| 録音した音声 | 端末内 IndexedDB のみ。外部へ送信しない。20本まで。設定で保存を切れる |
| Googleのトークン | メモリのみ。localStorage に保存しない |
| リポジトリ | GitHub Pages 用に Public。**実データ・スクリーンショット・取引先名を含むサンプルをコミットしない** |
| サンプルデータ | 架空の会社名・品番を使う |
| バックアップ | Settings からの JSON 書き出し／取り込みで対応（端末故障・キャッシュ削除への備え） |

**リポジトリを Public にする以上、コミット前に取引先名が混入していないか必ず確認すること。**
Private リポジトリ＋Cloudflare Pages に切り替える選択肢も残す。

---

## 9. 段階計画

### Phase 0 — 試作（完了）

React 単一ファイルで、音声入力 → AI構造化 → 確認 → 一覧 の一連の流れを検証済み。

### Phase 1 — 自分専用 PWA

**目標**: 毎日実際に使える状態にする。

- [x] リポジトリ作成、Vite + React + TS の初期構築
- [x] `types.ts` と `Repository` インターフェースの確定
- [x] `LocalRepository`（IndexedDB）実装
- [x] 音声入力・キーボード入力・フォーム入力の3経路
- [x] ReviewSheet（確認・修正）
- [x] 一覧ビュー（今日／今週／すべて／完了）
- [x] PWA 化、GitHub Pages へのデプロイ設定（Android ホーム画面への追加は実機で確認）
- [x] JSON バックアップ書き出し／取り込み

**完了条件**: 1週間、紙のメモを使わずに運用できること。

### Phase 2 — 出口を増やす

- [x] スケジュールビュー（勤務時間のタイムライン＋2週間の日付軸）
- [x] Web Share Target（他アプリからの共有）
- [x] Googleカレンダー書き出し（URL / ICS）
- [x] 日報用テキスト書き出し
- [x] CSV書き出し
- [x] 分析ビュー（§6.2.2b）
- [x] 実績ビュー（§6.2.2c。実行中の記録・やった業務の後入れ）
- [x] 停止後の高精度文字起こし（端末内 Whisper。§6.1.1）

### Phase 2.5 — 実績・予定・カレンダー（v1.14.0 / v1.18.0）

- [x] 実績（`Task.startedAt` / `actualMin`）— 手を付けた時刻とかかった時間
- [x] 予定（Plan）— タスクとは別の台帳。打合せ・来客・固定の業務
- [x] カレンダー（月表示）— 予定とタスクを同じ升目に並べる
- [x] 予定の実行（開始・一時停止・終了）— 区間で持つ。同時に何本でも
- [x] 予定の自動計上（既定は自動・予定ごとに手動へ切り替え）
- [x] 区分から1件立てて始める（飛び込みの作業）
- [x] 日報に予定の時間を出す（台帳には混ぜず、書き出しで足す）

**完了条件**: 日報に書く時間を、記憶ではなく記録から拾えること。

### Phase 3 — 共有

- [ ] `SheetRepository` 実装（GAS + Googleスプレッドシート）
- [ ] 担当者フィールドの追加
- [ ] 差分同期と競合解決（最終更新優先）
- [ ] 共有範囲の設計（誰のタスクが誰に見えるか）

**方針**: 社内は Google Workspace のため、共有先は GAS + スプレッドシートとする。
管理職がスプレッドシートを直接開けることを利点として活かす。

---

## 10. 判断の記録

後から「なぜこうしたか」を思い出せるように、迷った点と結論を残す。

| 論点 | 結論 | 理由 |
|---|---|---|
| ネイティブ vs PWA | PWA | 1コードベースで Android と PC を賄える。ストア審査が不要で、直したその日に反映できる |
| 音声認識をAPI（Whisper等）にするか | ブラウザ内蔵 | 無料、遅延が小さい、そして何より音声データを外に出さずに済む。iOS 対応が必要になった時点で再検討。**v1.17.0 で「既定は端末内のまま、利用者が自分のキーを入れたときだけ Gemini も選べる」に変えた**（§10.1） |
| AI構造化を全自動にするか | 確認を挟む | 期限の誤解釈が黙って登録される損失が大きい。ユーザーの明示的な希望でもある |
| 保存先を最初からクラウドに | しない | 個人利用の段階でサーバを持つと運用が重い。インターフェースだけ切っておけば後から足せる |
| 名前に Voice を入れるか | 入れない | 音声は入口の一つに過ぎない。多入力・多出力という本質を名前に持たせる |

### 10.1 実装時に決めたこと（v1.0 実装で追記）

| 論点 | 結論 | 理由 |
|---|---|---|
| 見た目を §7 の伝票調のままにするか | **NoteLoop 寄りに変更** | 利用者の指示。同じ作者の NoteLoop と操作感を揃えると、2つのアプリを行き来しても迷わない。等幅・色帯・角丸の抑制（§7.2〜7.3）は残し、面の作り（ヘッダー、ガラス面のパネル、下部固定のマイク）を NoteLoop に合わせた |
| どの版の NoteLoop に合わせるか | **9.2（現行）のダークテーマ** | 初回は既定ブランチ（`claude/app-creation-5z8ppb`）の明るいグラデーション版を見て作ってしまった。実際に配信されているのは `main` の 9.2 で、マットなグレーがかった黒がベース。利用者の指示で現行に合わせ直した。以後 NoteLoop を参照するときは **`main` ブランチ**を見ること |
| 優先度の色を §7.1 のまま使うか | **色相を保ったまま明度を上げる** | §7.1 の値（#B03A28 / #B87A22 / #3F7194）は白地の伝票を想定したもので、#17181b の暗い面ではコントラストが足りない。色相は保ちつつ #ff7a6b / #e2a33f / #78aed6 に上げ、WCAG AA（4.5:1）を満たすことを確認した |
| 明るいブランド面の上の文字色 | **墨（#17181b）を載せる** | ダーク版のブランド色（#7b93ff / #a68cff）は明度が高く、白文字だとコントラストが取れない。ボタン・タブ・チェックなどブランド色で塗る面はすべて墨文字にした |
| グラフの作り | **StudyDeck の成績画面に合わせる** | 利用者の指示。棒＋折れ線を1図にまとめ、目標線を1本引く形は既に使い慣れている。ゼロから別の見せ方を作る理由がない |
| `Task.estimateMin` の追加 | **追加する** | 勤務時間（8:20〜17:10）を設定した以上、「その日に収まるか」を出せないと設定が飾りになる。null 許容にし、未見積は `defaultEstimateMin` で埋めるので既存データは壊れない |
| 勤務時間を Task 側に持つか | **Settings に持つ** | 勤務時間はタスクの属性ではなく利用者の属性。タスクごとに持たせると全件更新が必要になる |
| AIプロキシ未設定時の挙動 | **端末内のかんたん解析にフォールバック** | プロキシを立てる前でもアプリが使えないと、Phase 1 の完了条件（1週間、紙のメモを使わずに運用）に届かない。フォールバック経路も必ず ReviewSheet を通すので、原則1（AIは提案し、人が決める）は崩れない。どちらの経路で解釈したかは確認画面に明示する |
| 音声認識の実装 | **NoteLoop の作りをそのまま踏襲** | `continuous` + `interimResults` で、認識が勝手に切れたら確定分をコミットして張り直す。この挙動は NoteLoop で実地検証済みで、作り直す理由がない |
| 分析ビューの追加（§6.3 の画面一覧に追加） | **追加する** | 利用者の指示。`source` を記録する設計上の意図（どの入口を育てるか）が、見る場所がないと活きない |
| 公開パスの大小 | **`/Taskport/`（リポジトリ名に合わせる）** | design.md 初版は `/taskport/` を想定していたが、実際のリポジトリ名は `Taskport`。GitHub Pages のパスがリポジトリ名の大小を区別する場合に全アセットが 404 になるため、想定URLの方をリポジトリ名に合わせた |
| 昼休憩の既定値 | **12:25〜13:05（実働 8時間10分）** | 初回は 12:20 開始で作ったが、実際の勤務は 12:25 開始。利用者の指摘で修正した |
| 録音の作りをどこまで NoteLoop に合わせるか | **画面・通知・音声保存まで合わせる** | 利用者の指示。同じ端末で2つのアプリを使うので、録音中の見え方と操作が違うと迷う。NoteLoop 9.2 で実地検証済みの仕組み（3秒ごとのデータ確保・見張りと録り直し・無音再生によるバックグラウンド維持）をそのまま持ってきた。出口だけ差し替え、議事録ではなくタスク候補を作る |
| 録音した音声を残すか | **既定で残す（20本まで）** | 「登録されたタスクが言ったことと違う」ときに元を確かめられないと直しようがない。端末内にのみ置き、外部へは出さない。容量を食うので本数を絞り、設定で切れるようにした |
| Googleカレンダーへタスクを送るか | **送る（利用者が選んだ分だけ）** | 利用者の指示。§8 の「タスクデータをサーバに置かない」と衝突するため、**自動送信はしない**・押したときに選んだ分だけ送る・画面に「Google に渡ります」と明示する、の3点で限定した。読込側は台帳に混ぜず、取り込みも確認画面を通す |
| クライアントIDをアプリに埋め込むか | **埋め込まない** | 公開リポジトリなので、利用者が自分の Google Cloud で作ったIDを設定画面に入れる方式にした。承認済みの生成元で縛られるため、IDが漏れても他所からは使えない |
| 外部AIで自然文を解析するか | **やめる（端末内の解析のみ）** | 利用者の指示。取引先名・品番・数量を含む文章が端末から出なくなり、鍵の管理もプロキシの運用も要らなくなる。代わりに期限や優先度の読み取りは素朴になるため、確認画面での確認がこれまで以上に要になる。Cloudflare Workers のプロキシは削除した |
| IndexedDB の版を上げるか | **上げない（保存先を分ける）** | v1.2.0 で録音を足す際に版を1→2へ上げたところ、別のタブが古い版を掴んでいるとアップグレードが永久に待たされ、「読み込んでいます…」から進めなくなった。版を固定するのをやめ、台帳は版を指定せず開き、録音は別DB（`taskport-media`）に分けた。以後、保存先が増えてもDBを足すだけで台帳には触らない |
| 録音中を全画面にするか | **画面下半分のポップアップ** | 利用者の指示。全画面だと録音中に一覧が見えず、「いま何が残っているか」を確かめながら喋れない。見た目は NoteLoop 9.2 のまま（経過時間・縦バーのゲージ・3つのボタン）にして、出し方だけ変えた |
| 昼休憩のほかに小休憩を持つか | **持つ（既定 10:20〜10:25 / 15:05〜15:10）** | 利用者の日報の様式から判明した。始業〜終業から昼休憩だけを引くと 490 分になるが、日報の枠は 16 コマ×30 分＝**480 分**で組まれている。差の 10 分は午前と午後の 5 分休憩。稼働率・空き時間・タイムラインのすべてがこの値に依存するので、`WorkHours.shortBreaks` を足して実働を 8 時間ちょうどに合わせた。設定画面で増減できる |
| 区分をマスタ管理するか | **する（§5.1 の判断を差し替え）** | §5.1 は「自由入力とし、マスタ管理はしない。実運用で頻出する語が固まってから候補化する」としていたが、日報の様式として既に固まっていた。その語をそのまま候補にする（8大分類・35小分類、`src/lib/workCategories.ts`）。自由入力は残し、マスタ外の語は「その他」として集計する |
| 自然文から区分を当てるか | **語のみで当てる（外れたら空欄）** | 「発注」「棚卸」「見積」のように区分がほぼ一意に決まる語だけを表に持ち、当たらなければ空欄のまま確認画面に出す。推測で埋めると誤った区分が黙って集計に入るので、当てないほうを既定にした |
| 分析の「区分ごとの時間」を実績とするか | **見込み時間の集計であると画面に書く** | 台帳が持っているのは見積（`estimateMin`）であって実際にかかった時間ではない。日報の集計（0.25 時間×コマ数）と同じ見え方をするため、そのまま出すと実績と誤読される。カード内に「完了したタスクの見込み時間の集計で、実際にかかった時間ではありません」と明示した |
| 業務日報の書き出し形式 | **タブ区切り（TSV）** | エクセルへ貼るのが唯一の用途。CSV だと区分名に含まれる読点がセルを割ってしまう。時刻の枠は日報と同じ 16 コマで、30 分を超えるタスクはコマ数ぶんに広げ、時刻の無いタスクは空いた枠へ順に置く |
| 一覧の探し方をタブだけにするか | **検索と絞り込みを別に置く** | タブ（今日/今週/すべて/完了）は「いつやるか」で切る道具で、「あの件どうなったか」を探すのには向かない。台帳が数百件になると届かなくなるので、件名・メモ・区分・手順を横断する検索と、区分（大分類）・優先度・期限での絞り込みを足した。**絞り込み中はタブを離れて全件から探す**（「今日」に立ったまま来月の1件を探して0件、という迷い方を防ぐ）。よく使う条件は名前を付けて残せる（`Settings.savedFilters`） |
| 繰り返しの単位をどこまで持つか | **5つだけ（毎日／稼働日ごと／毎週／毎月おなじ日／毎月末）** | 「2週おき」のような間隔は持たせない。日報・週次会議・棚卸・月初集計という実際の定例がこの5つで足りる。選択肢が増えるほど確認画面が重くなる |
| 繰り返しの次回をいつ作るか | **完了にした時点で1件だけ** | 先の分をまとめて作ると、一覧が未来のタスクで埋まって「いま何をやるか」が見えなくなり、予定が変わったときに作り置いた分を全部直すことになる。1件ずつなら、やめたければその1件を消せば終わる。遅れて完了したときは過ぎた回を飛ばし、前倒しで完了したときはその回を消費して次へ進む |
| サブタスクを何階層にするか | **1階層（手順の中に手順は作らない）** | 階層が増えると、いま何をやるかを探す手間のほうが大きくなる。カードから開いてその場でチェックできるようにし、1つ潰すたびに編集画面へ入らずに済ませた。手順を全部済にしてもタスクは完了にしない（完了にするのは人の操作） |
| リマインドをどう配るか | **端末内だけ。2つの道を実行時に選ぶ** | プッシュサーバを持つと §8 の「タスクデータをサーバに置かない」が崩れる。予約つき通知（Notification Triggers）が使える端末では7日先までを Service Worker に預け、使えない端末ではアプリを開いている間のタイマーで出す。**どちらも取りこぼしがある**ので、いまどちらで動いているかと出ない条件を設定画面にそのまま書く。既定は切 |
| 保存データを開くのを時間で打ち切るか | **打ち切らない（v1.6.1 で変更）** | v1.2.1〜1.6.0 は「8秒で開かなければ失敗」にしていた。ところが実機（Android・ホーム画面から起動）では、健全なDBでも起動直後の混み合いで8秒を超えることがあり、しかも諦めるときに開きかけの接続をこちらから閉じていたため、あと少しで開くはずのものを自分で壊していた。**待ちは打ち切らず、時間がかかっていることだけを画面に伝える**方式に変えた。開けた時点で画面が直る |
| 保存データが開けないとき、画面をどうするか | **画面は動かし、上に理由を出す** | 1つ開けないだけで全部の画面が使えなくなると、書き出しにも設定にも手が届かなくなる（v1.6.0 の実機がこの状態）。台帳は空のまま画面を出し、「いまタスクは読めておらず、新しい登録も保存できない」ことと、ブラウザが返した素の理由・空き容量を並べる。最後の手段として「保存データを作り直す」を置いた（消えることを先に伝えてから実行する） |
| 地の色を黒にするか白にするか | **白（v1.7.0 で変更）** | 利用者の指示。「黒背景が見にくい」。現場と事務所を行き来する使い方では、明るい場所で暗い面はまぶしさに負けて読めなくなる。面は白〜ごく薄いグレー、文字は墨にした。**ヘッダーと引き出しだけは色面（藍〜菫）のまま**にしてある。画面の上端に色があると Android のステータスバーと継ぎ目なく繋がり、どのアプリを開いているか一目で分かるため。優先度の色は §7.1 の値（白い伝票を想定した色）に戻した。ただし §7.1 の中 `#b87a22` は白地で 3.6:1 しかないので `#8a5a12` に落とし、全色で WCAG AA（4.5:1）を満たすことを確かめた |
| Googleカレンダーの設定をどこまで案内するか | **コンソールへ直接飛べる4手順にする** | Google Cloud の画面は階層が深く、「APIを有効化 → 同意画面 → クライアントID」の順にたどるのが毎回手間になる。設定画面に手順を並べ、それぞれのコンソールへのリンクと、承認済み生成元をそのまま写せるコピーボタンを置いた。リンクを押した時点では何も送らない |
| タスクをサーバに置くか（§8 の見直し） | **置く（Google Drive のアプリ専用フォルダ・既定は切）** | 利用者の指示「スマホとPCで同時に追加・完了させたい」。§8 の「タスクデータをサーバに置かない」を、ここで初めて崩す。置き場は Drive の `appDataFolder`（マイドライブに出てこず、ほかのアプリからも Drive の画面からも見えない）。**タスクの件名・メモ・区分が Google に保存される**ので、既定は切、設定で入れたときだけ動く、設定画面にそのまま書く、の3点で限定した。音声は今までどおり外へ出さない |
| 同期の置き場をどれにするか | **Drive（GAS＋スプレッドシートではなく）** | design.md の Phase 3 は GAS＋スプレッドシートを想定していたが、Drive なら**カレンダー用に作ったクライアントIDをそのまま使い、スコープを1つ足すだけ**で済み、デプロイするものが無い。中身を目で見たいという要望が出たら、書き出し（JSON/CSV）でも足りる |
| 同時に触ったときの決着 | **1件ずつ、更新時刻の新しいほう（墓標つき）** | ファイル全体を上書きすると片方の操作が丸ごと消える。ID ごとに `updatedAt` を見て新しいほうを採り、削除は墓標（ID→消した時刻）で伝える。**同じ1件をほぼ同時に直すと、あとから保存したほうだけが残る**（項目ごとには混ぜない）。一人で2台という前提での割り切りで、設定画面にも書いた。墓標は90日で掃除する |
| 設定も同期するか | **しない（タスクだけ）** | 勤務時間や保存した絞り込みは端末ごとの都合が入る（PCと現場で違う枠を使うこともある）。混ぜると衝突の解決が要る割に得が小さい。設定の持ち運びは JSON バックアップで足りる |
| タスクを時間帯に割り当てるか | **割り当てる（タイムボックス）** | 一覧として持つだけでは「その日に本当に入るか」が全部やってみるまで分からない。枠ごとに見積もりの合計と残りを出し、詰め込みすぎを朝の時点で気づけるようにした。枠は**勤務時間の区切りそのもの**（＝日報の時間区分）。独自の時間割を作らないのは、日報へ書き写すときにずれるため。粗さは「帯」まで（分刻みにすると少し狂っただけで全部組み直しになる） |
| 次にやる1件を選ぶ手間 | **アプリ側が1件だけ出す** | いまの枠の先頭を1件だけ大きく出し、終わると次に入れ替わる。何をやるか毎回選び直す時間をなくす（シングルタスク） |
| 溜まった超過をどう片づけるか | **朝の仕分け（1件ずつ4択）** | 一覧の中で1件ずつ開いて直すのは手間がかかり、結局そのまま溜まる。今日やる（枠も選ぶ）／明日へ／期限を外す／もう終わっている の4つに絞った。**開いた時点の一覧を写し取って**回す（さばくたびに「超過」でなくなって列が縮み、次を飛ばすため） |
| 録音ゲージを何で動かすか | **マイクの実音量（RMS）**（v1.9.0 で変更） | v1.8.0 まで「音声認識が結果を返したら 0.85 にする」だけで動かしていたため、認識が返ってこない場面（通信が細い・非対応・喋り始めた直後）ではゲージが止まって見えた。録音に使っている流れをそのまま分岐させて実測に変えた。録音を残さない設定でもゲージのためだけにマイクを取り、止めるときに必ず手放す。音は測るだけで外へ出さない |
| 勤務日を曜日だけで決めるか | **会社カレンダーを実日付で持つ** | 祝日・一斉有給・年末年始は平日でも休みで、逆に土曜出勤の日もある。曜日の設定だけでは稼働率も空き時間も合わない。会社が配るカレンダーがその答えなので、休日と出勤日を実日付で持ち、曜日より優先する。判定は「出勤の指定 → 休みの指定 → 曜日」の順（休みの週に1日だけ出る、を表せるようにするため） |
| 会社カレンダーの入れ方 | **Googleカレンダーからの取り込みと、月の枠の手入力の両方** | 会社のカレンダーを Googleカレンダーに追加してあるなら取り込みが速い（終日予定だけを見て、件名から見当づける。祝日の名前も拾う。当てられないものは人が選ぶ）。紙で配られただけなら、月の枠を押して1日ずつ決める。年間の稼働日数と休日数を出しているのは、紙の数字と突き合わせて入れ間違いを自分で確かめられるようにするため |
| 会社カレンダーを同期に乗せるか | **乗せる（設定の中でこれだけ）** | 両方の端末で違う勤務日を使うと、稼働率も空き時間も食い違う。丸ごと更新時刻の新しいほうを採る（1日ずつは混ぜない）。勤務時間や保存した絞り込みは端末ごとの都合が入るので、今までどおり同期しない |
| 画面を端末ごとに変えるか | **変える（1024px と 1180px で切り替え）** | 利用者の指示と、参考にした画面（Smartsheet のダッシュボード）。**PC** は左にナビを出しっぱなしにし、一覧を「左＝その日の状況／右＝探して片づける」の2カラムに、分析はカードを2〜3枚ずつ並べる。文字入力欄は最初から開けておき、キー操作（`/` 探す・`n` 直接入力・`e` 書き出し・`1〜4` タブ・`Esc` 閉じる）を足した。**スマホ** は1カラムのまま、検索とタブをヘッダーの下に貼り付けて、一覧が長くなっても手が届くようにした。端末の見分けは幅だけでなく「細かく指せる装置があるか」も見る（タブレットは広くても指で触るため） |
| 会社の休日をコードに書くか | **書く（2026年7〜12月ぶん・CLAUDE.md §3.1 の明示的な例外）** | 利用者の指示「カレンダーは CLAUDE.md を無視してでも休日を入れて」。§3.1 は「実データをコミットしない」だが、紙のカレンダーを毎回手で入れ直すのが実務の負担になっていた。**会社名は入れず日付だけ**を持ち、どこの会社のものか分からない形にしてある（`src/lib/calendarPresets.ts`）。写し取りである以上、読み違いが黙って稼働率を狂わせるので、(1) 平日の休みがすべて祝日の名前か紙の一斉有給と一致すること (2) 7〜12月の稼働122日・休62日が紙の「年間稼働251日・年間休日114日」と矛盾しないこと、の2つで裏を取り、取り込み画面に**月ごとの内訳**を出して紙と1行ずつ突き合わせられるようにした。1〜6月は紙が無いので入っていない |
| 写真から休日を読む方法 | **文字は読まず、色だけを見る** | OCR は小さく写った写真では当てにならず、外部の文字認識に送るのは §3.1（外へ出さない）に反する。一方「何日がどのマスか」は年と月から計算で決まる（1日の曜日と日数が決まれば並びは一意）。だから写真から要るのは「そのマスが赤いか」だけで済む。利用者が日付の並ぶ範囲を四角で囲み、7列×n行に割って、各マスのインクの赤み（R −(G+B)/2）を測る。しきい値は決め打ちにせず、測った値のいちばん広い隙間で切る（色かぶりで総崩れになるため）。**読んだ結果はそのまま保存せず、月の枠に出して押して直せる**（§10.1 の「確認を省かない」と同じ扱い）。マスの内側は 84% を見る — 紙の数字はマスの左上に寄っていて、中央だけ見ると1桁の日（1・2・8…）を落とす |
| 区分を複数持てるようにするか | **持てるようにする（v1.11.0）** | 利用者の指示。1つの作業が「発注」と「納期確認」の両方にまたがることが実際にあり、1つしか選べないと集計が実態とずれる。`Task.category: string` を `categories: string[]` に変えた（読み込み時に古い1件を配列へ寄せるので既存データは壊れない）。**時間の集計は先頭（主区分）にだけ積む** — 付いている区分すべてに同じ時間を積むと、合計が実際の勤務時間を超えて稼働の判断が狂う。件数の集計は付いている区分すべてで数える（件数は重複してよい） |
| 区分のマスタをコードに固定したままにするか | **設定へ移し、画面から編集できるようにする（v1.11.0）** | 利用者の指示。仕事は増えるし名前も変わる。コードを直さないと足せない形にしておくと、いずれ「その他」ばかりになって集計が死ぬ。マスタは `Settings.categoryGroups`（グループ＝名前・色・区分の並び）に持ち、区分の選択画面と設定画面の両方から直せる。**マスタの編集はその場で保存する**（選んだ内容は ✓ まで仮）。「足したのに閉じたら消えた」を起こさないため |
| 区分の選び方 | **押すと出る画面で、グループごとに畳んだ階層から選ぶ** | 利用者の指示。既定で35件あり、平らな候補（datalist）では探せないし、複数選ぶ操作もできない。グループ見出しを押して開き、中の区分を押して選ぶ形にした。検索も付けてある。新しい区分は同じ画面の下から足せて、**行き先グループは語から自動で見当づける**（違えば選び直す） |
| グループに色を持たせるか | **持たせる（tokens.css の `--cat-*` から10色）** | 一覧のチップ・絞り込み・分析のグラフで同じ色になるので、どの塊の仕事が多いかが色で分かる。値そのものはトークンにしか置かず、設定は色の**名前**（indigo など）だけを持つ（CLAUDE.md §4.4「16進数を書かない」を保つ）。白地で文字として読める明度に揃えてある |
| 件名から区分を自動で入れるか | **入れる。ただし人が触ったら以後は触らない（v1.11.0）** | 利用者の指示。毎回選ぶのは手間で、結局「未分類」が溜まる。件名を打つたびに語から当てて入れ、当たらなければ空のままにする（推測で埋めない方針は変えない）。自分で選び直した時点で自動は止まる — 選んだものを、件名を直した拍子に上書きされるのが最悪の挙動だから。当てるのは**2件まで**（増やすほど外れも増える） |
| 一度作ったタスクを記憶するか | **記憶する（登録した時点で自動的に控える）** | 利用者の指示。同じ作業を毎回打ち直している。件名・メモ・区分・優先度・見込み時間・時間枠・手順を控え、タスクを作る画面の「記憶から呼び出す」で埋める。**期限は控えない**（毎回違うし、古い日付のまま登録されると黙って超過扱いになる）。同じ件名は1件にまとめて回数を数え、よく使う順に出す。置き場は台帳と同じ meta ストア（DBの版は上げない）。端末内にのみ置き、同期にも書き出しにも乗せない |
| 決定・取消のボタンを言葉にするか | **✓ と ✕ の記号だけにする（v1.11.0）** | 利用者の指示。片手で持って何度も通る画面なので、読まずに押せるほうが速い。記号だけにするぶん、`aria-label` と `title` に何が起きるかを必ず持たせ、読み上げと長押しでは言葉が出るようにした |
| 下部の統合バーを残すか | **廃止し、左下＝録音・右下＝＋ の2つだけにする（v1.11.0。左下は v1.12.1 で外した）** | 利用者の指示「キーボード機能がよくわからない」。入口が3つ（キーボード・マイク・直接入力）に割れていて、どれを押すと何が起きるのか分からなくなっていた。バーごと外し、**自然文の入力は「＋」の中（タスクを作る画面）の「文章から作る」へ移した**。入口は増やさず、置き場所を1つにまとめただけで、解析は今までどおり parseToTasks を通り確認画面に出る。PC で文字入力欄を開いたままにする作り（v1.9.0 の判断）も、これに合わせてやめた |
| 一覧の「本日の勤務」の見出し | **TODAY にする** | 利用者の指示。日本語の見出しが3つ縦に並ぶと、どれが何の枠か区別しづらい。ここだけ英字・等幅にして、その日の枠であることを一目で分かるようにした |
| ＋から作り方をどう選ばせるか | **＋の周り 1/4 円に4つを扇で開く（v1.12.0）** | 利用者の指示とスケッチ。＋を押すといきなりフォームが開く作りだと、記憶・文章・音声へは中の階層をもう1段たどることになる。真上から左へ 手描き・記憶・文章・マイクの順に 30°ずつ置き、親指の届く範囲（右下から左上への扇）に収めた。名前は円の外側に出す（下に置くと隣とぶつかり、真横のものは画面の下に切れる） |
| 扇を長押しでどう扱うか | **押したまま滑らせて、離した先が動く（v1.12.0）** | 利用者の指示。長押しで開くだけだと、押す→離す→狙って押す の3手になる。指を置いたまま開き、そのまま滑らせて離せば1手で済む。指の下のアイコンは大きくして色を反転させ、どれが動くかを離す前に見せる。**どのアイコンにも乗せずに離したときは開いたまま**にする（狙いを外しただけで閉じると、長押しからやり直しになる） |
| 扇にマイクを入れたとき、左下の録音ボタンをどうするか | **外す（v1.12.1）** | 利用者の指示。いったんは「残して、扇が開いている間だけ隠す」で作ったが、同じ形の丸が画面に2つあること自体が迷いのもとだった。録音は扇のマイク（長押しのまま左へ滑らせて離す）に一本化した。歩きながらの1手は、長押し＋スライドで保てている |
| アプリアイコンの地をどう塗るか | **512×512 の全面を塗り、角丸は端末に任せる（v1.12.1）** | 利用者の指摘「アイコン背景が2重構造になっていて小さい」。絵の中に白い角丸カードを描いていたため、Android がさらに角丸で切り抜いて枠が二重になり、そのぶん中身が小さく見えていた。地はブランドの藍〜菫で全面を塗り、絵は maskable の安全域（中央 80% の円）に収めた。元絵は `public/icons/icon.svg` の1枚で、`node scripts/makeIcons.mjs` が用途ごとに倍率を変えて PNG を焼く（ファビコンは 32px で潰れるので少し大きく描く） |
| ＋の4つを画面としてどう分けるか | **手描き・記憶・文章をそれぞれ独立した画面にする（v1.13.0）** | 利用者の指示。v1.11.0 では「タスクを作る」画面の中に記憶と文章を抱えていたが、扇で「記憶」を選んでもフォームが開き、その中の小さなボタンをもう一度押すことになっていた。扇で選んだものがそのまま開くようにし、フォームは「手描き」1つの仕事に戻した。記憶から選んだときは、中身の入ったフォームが開く（期限だけ入れれば済む） |
| 扇のボタンの大きさと動き | **＋と同じ 60px。出るときは行き過ぎてから止まり、閉じるときは外側から順に吸い込まれる** | 利用者の指示。小さいと押し外す。同じ大きさに揃えたぶん半径を 136px に広げた。動きは出入りで別のカーブにしてある（出る＝overshoot、閉じる＝加速して吸い込む）。`prefers-reduced-motion` では動かさない |
| 録音中のゲージの色と大きさ | **黒・高さ 150px（v1.13.0）** | 利用者の指示。ブランド色の細いゲージは、離れた場所（現場で端末を置いたまま喋る）だと声が乗っているか分からなかった。黒にして高さを倍近くに取り、経過時間や案内の文字も黒に統一した。赤は録音中を示す丸とボタンだけに残す |
| TODAY カードに何を出すか | **積んだ時間だけ（v1.13.0）** | 利用者の指示。始業・終業・昼休憩・小休憩の時刻を毎日出しても読まない（変わらないため）。「45分 / 8時間」だけを大きく出し、カードを2行に詰めた。時刻は設定で見られる。すぐ下の「今日の進めかた」との間にも間隔を入れた（スマホでは列に間隔が無く、地続きに見えていた） |
| ヘッダーと引き出しの色 | **ヘッダーは明るいグレー、引き出しは暗い墨（v1.13.0）** | 利用者の指示。藍〜菫の色面は、白地の本文と並ぶと上端だけが浮いて落ち着かなかった。ヘッダーを地に近いグレーにして、色は一覧の中（優先度・区分）だけに残す。引き出しは逆に暗く落として、開いたときの切り替わりを出す。Android のステータスバーに合わせる `theme_color` もグレーへ変えた |
| 書体をどうするか | **Inter（ラテンのみ・48KB）を同梱し、日本語は端末のもの（v1.13.0）** | 利用者の指示「おしゃれなフォント」。日本語の書体を同梱すると数MBになり、圏外での起動が重くなる。ラテンと数字だけを Inter（可変・ラテンサブセット）に替えると、数字・時刻・見出しの印象が大きく変わるわりに 48KB で済む。**外部（Google Fonts）へは取りに行かない**（同梱して Service Worker のキャッシュに乗せる）。等幅フォントに切り替えていた箇所は、Inter の等幅数字（`tabular-nums`）に替えた。桁は揃ったまま、字面が本文と揃う |
| アイコンとファビコンの地の色 | **ほんの少しだけくすんだ白（`#f2f4f8`）にし、絵を藍〜菫で塗る（v1.13.1）** | 利用者の指示。アプリ本体が白地なので、アイコンだけ藍〜菫の色面だと、ホーム画面でもタブでも別のアプリのように見えていた。地と絵の明暗を入れ替え、地は白地の系統（`--bg` の近く）に置く。**真っ白にしない**のは、白い通知シェードや白いタブの上で輪郭が消えるため。元絵は `public/icons/icon.svg` の1枚だけなので、ここを変えるとファビコン（32/48px）も同じ地になる |
| 期限のないタスクに繰り返しを付けられるようにするか | **付けられるようにする（v1.14.0）** | 利用者の指示。定常業務には締切という感じのものが無く、繰り返しを設定するためだけに今日の日付を入れると、翌日には黙って超過になる。期限が無いときは**完了にした日**を起点に次回を数える。次回ぶんには日付を入れる（入れないと「毎日」と「毎週」の区別が付かず、完了した端から同じものが戻ってくる）。自然文の解析でも、回りかたしか言っていない文に今日を入れるのをやめた |
| 実績（実際にかかった時間）をどう持つか | **`startedAt` と `actualMin` をタスクに足す（v1.14.0）** | 利用者の指示「実行中の業務・過去の業務を手軽に入力・編集したい」。別のストアにすると、同じ仕事が2か所に分かれて突き合わせが要る。台帳の1件のまま、**見込みとは別の欄**に持つ。同じ欄にすると、見込みを直したときに日報の数字が黙って変わる |
| 実行中をタイマーで測るか | **押した時刻を残すだけにする** | タイマーを持つと、画面を閉じた・端末が寝た・アプリが落ちた、のたびに数え直しの問題が出る。着手時刻を1つ残しておけば、いつ見ても経過は引き算で出る。完了にした時点で実績に落とす |
| 日報に出す仕事をどう選ぶか | **完了した日で見る（v1.14.0）** | それまでは期限がその日のものだけを拾っていたので、**昨日ぶんを今朝片づけた1件がどちらの日の日報にも出なかった**。完了したものは完了日、手を付けたものは着手日、まだのものは期限で拾う |
| ISO の時刻から日付を取るのに `slice(0, 10)` を使ってよいか | **だめ。`dayOfIso()` を通す（v1.14.0）** | ISO は UTC なので、日本時間の朝 8:20 に完了したタスクは `…T23:20:00Z`（前日）になり、その日の集計から丸ごと落ちる。始業が 8:20 のこのアプリでは毎朝ぶんが消える。分析・連続日数・日報・区分ごとの時間が全部ずれていた |
| リアルタイム文字起こしと高精度出力を両立できるか。PWA で足りるか | **PWA のままで足りる。アプリ化はしない（v1.15.0）** | 利用者の指示「NoteLoop同様の仕組み。PWAで難しければアプリ化」。NoteLoop 9.2 の `main` を読むと、ブラウザでは **Web Speech（字幕）と MediaRecorder（録音）を同時に動かし**、停止後に録音音声を端末内 Whisper へ通して確定版にしている。Taskport はすでに前半（同時録音＋字幕）ができているので、足りないのは後半だけだった。NoteLoop が Capacitor でアプリ化しているのは**フォアグラウンドサービスで録音を切らさないため**で、文字起こしのためではない。アプリ化すると配信（GitHub Pages）と更新の仕組みが丸ごと変わるので、必要になるまでやらない |
| 端末内 Whisper をどう組み込むか | **`public/` に素の JS の Worker を置き、transformers.js を CDN から読む（NoteLoop の worker.js と同じ）** | npm で入れると onnxruntime の `.wasm` をどこから配るかを自前で面倒みることになり、ビルドも重くなる。CDN から入れれば同じ CDN の中で解決される。素の JS にしたのは、**使うと決めるまで 1 バイトも読み込まれない**ようにするため。版は URL に固定してあるので勝手に上がらない。npm の依存は増えない（CLAUDE.md §2） |
| 停止後に自動で走らせるか | **押したときだけ（v1.15.0）** | 利用者の指示。スマホの CPU では1分の録音で数十秒〜数分かかる。自動にすると、ひとこと吹き込むたびに登録が止まる。速報は Web Speech の文字でそのまま登録し、取りこぼしが気になったときだけ取り直す |
| 既定のモデルをどれにするか | **base（約80MB）** | 利用者の指示。small（約250MB）はスマホで目に見えて遅く、初回の取り込みも重い。Web Speech の取りこぼしを埋める用途なら base で足りる。設定で tiny / small / large-v3-turbo にも変えられる |
| 1日のスケジュールをタイムラインで見せるか | **やめて、勤務時間の枠ごとに畳んだ表にする（v1.16.0）** | 利用者の指摘「スケジュールがなんか見にくい」。1分＝0.9px で置くと、3件しか無い日でも画面いっぱいの縦長になり、そのほとんどが空白だった。さらに**時刻を決めていない仕事はタイムラインに置き場が無く**、下端の「時間未指定」へ追いやられて画面外に消えていた。Taskport は時刻を入れない仕事のほうが多いので、これは致命的だった。枠（＝日報の時間区分）ごとに畳むと1日が1画面に収まり、時刻の無い仕事も同じ面に並び、「この枠はもう埋まっているか」まで同時に読める。枠の見た目は「今日の進めかた」と同じものを使い回す（同じ考えのものは同じ形に見せる） |
| 外部のAIサービスを呼ばない方針を続けるか | **利用者が自分のAPIキーを入れたときだけ Gemini を使えるようにする（v1.17.0）** | 利用者の指示。「文字起こし・要約の Gemini の設定が無い」。もともとは「外部のAIサービスを呼ばない」を §3.1 の絶対条件にしていた（取引先名・品番・数量が外へ出るため）。方針を変えるにあたって、**端末内で完結する道は全部残した**（Web Speech・端末内 Whisper・localParse）。守るのは4つ: 既定は切／キーが無ければ動かない・入口も出ない／押したときだけ送る／何が出ていくかをボタンのそばに書く。キーは設定（同期に乗る）ではなく端末内の localStorage にだけ置く |
| Gemini をどこで使えるようにするか | **録音の取り直しと、文章の解析の2か所** | 録音は「端末内で取り直す／Geminiで取り直す」を並べ、**押した側が相手を選ぶ**（設定の切り替えとは独立。音声が出ていくので毎回選ばせる）。文章の解析は毎回聞くと邪魔なので設定の切り替えにし、そのかわり**どちらで読んだかを確認画面に必ず出す**。Gemini が落ちたときは端末内で読み直し、そのことも画面に出す（黙って別の結果に差し替えない） |
| Gemini のモデルと落ち方 | **NoteLoop 9.2 と同じものにそろえる** | 同じキー・同じ無料枠を使うので、通るモデルと落ち方の癖をそろえておくほうが迷わない。既定は無料枠で実際に通る `gemini-3.5-flash`、404（提供終了）や429（枠切れ）のときは軽いモデルへ、5xx（混雑）は間を空けて同じモデルで粘る。長い音声の Files API は持たず、9分を超える録音は断って端末内 Whisper へ誘導する（音声メモにその長さは要らない） |
| ID生成に ULID ライブラリを入れるか | **入れない（26行の自前実装）** | 依存は最小に保つ（CLAUDE.md §2）。Crockford Base32 の時刻+乱数は自前で十分短く書ける |
| 予定（打合せ・固定の業務）をタスクと同じ台帳に入れるか | **別に持つ（v1.14.0）** | 利用者の指示。混ぜると2つ壊れる。(1) 完了の丸が付き、出ていない会議が「未完了」として残る (2) 見込み時間の積み上げと二重になる（予定の時間はもともと埋まっている）。**タスクは「やること」、予定は「その時間そこにいること」**として型ごと分け、稼働の計算では別に数える。Googleカレンダーから読む `CalendarEvent` ともさらに別物（あちらは読むだけ、予定は端末の中で作って直せる） |
| 予定の繰り返しをどう持つか | **作り置きせず、画面に出すときに展開する** | タスクの繰り返しは「完了にしたとき次の1件」だが、予定は完了にならないのでその手が使えない。12回ぶん作ると、時間が変わったときに全部直すことになる。起点の日と繰り返しの規則だけを1件持ち、月表示・スケジュール・実行のそれぞれが必要な期間だけ展開する（`occurrencesInRange`）。やめるときは1件消せば先の回も消える |
| 実行の記録（実績）の形 | **区間の並び（開始・終了のペア）** | 一時停止は区間を閉じるだけ、再開は次の区間を開くだけで済む。合計時間は足し算で出る。「開始時刻＋合計分」で持つと、止めた時間を引く計算がどこにでも散らばる |
| 同時に何本も動かせるようにするか | **何本でも動かせる（v1.14.0）** | 利用者の指示。電話を受けながら伝票を打つ、が実際に起きる。1本に絞ると「切り替え忘れ」で実績が嘘になる。そのかわり、2本以上動いているときは「合計が実時間を超える」と画面に書き、止め忘れに気づけるよう引き出しにも本数を出す |
| 予定の時間を自動で計上するか | **既定は自動。予定ごとに手動へ切り替えられる（v1.14.0）** | 利用者の指示。会議は始まりも終わりも決まっているので、そこで押させるのは手間なだけ。ただし予定どおりに始まらない仕事もあるので、予定ごとに手動へ倒せるようにし、既定値そのものも設定で変えられるようにした。**閉じている間に過ぎたぶんは、その日ぶんだけ**開始〜終了を埋める（会議に出ていたのに記録だけ空、を防ぐ）。前の日まで遡っては作らない — 出たかどうか分からないものを実績として書かないため。手で止めた記録・終えた記録には触らない（人の操作が常に優先） |
| 予定と実行ログの置き場 | **新しいDB（`taskport-work`）を足す** | 台帳（`taskport`）の版は上げない（CLAUDE.md §9）。DBを分けたので、こちらが開けなくてもタスクの読み書きは続く。実行ログは日付で持ち、90日より古いぶんは起動時に捨てる |
| 実行ログを同期・書き出しに乗せるか | **乗せない。予定は書き出しに乗せる** | 実行ログは「その端末で動かした実績」なので、別の端末へ移すと同じ時間が二重に立つ。予定は定例が消えると困るので JSON バックアップに入れた（`plans`。古いファイルには無いので、その場合は触らない） |
| 月表示の升目に何を出すか | **幅で出し分ける（狭い＝点、広い＝件名の帯）** | 7列に割ると1マスは 320px 幅で 44px しかない。そこへ件名を入れると全部が「…」になって、かえって読めない。狭い端末では予定＝四角・タスク＝優先度の色の丸を並べ、件数と重さだけを見せる。720px から件名入りの帯に切り替える。どちらも押せば下にその日が開くので、細かい確認はそちらでする |
| 区分から立てるとき確認画面を挟むか | **挟まない** | 確認画面が要るのは「AIが解釈したもの」（原則1）。区分から立てるのは人が押した1語がそのまま件名になるだけで、解釈が入らない。飛び込みの作業を数え始めるまでの手数を増やさない |
| 予定の実行と、タスクの実績（`startedAt`／`actualMin`）をどう一本化するか | **持ち場を分ける（v1.18.0）。タスクは台帳、予定は実行ログ** | 別々の作業で同じ「実行中」を2通り作ってしまい、突き合わせが要る状態になった（v1.14.0 が2つある）。片方に寄せるとどちらかが壊れる — タスクの実績は台帳に入って同期・書き出しに乗るべきもの、予定はそもそも台帳に置けないもの。そこで**記録の置き場を持ち物で決めた**。画面はどちらも同じ「実行」の面に出し、押す物の形も揃える。1つの仕事が2か所に載らないので、日報の時間も「いま動いているか」の判定も割れない |
| 「実行」と「実績」を別の画面にするか | **1つにする（v1.18.0）** | やることは同じ（いま動かす／動かした記録を見る）で、行き来が要るだけだった。上から いま動いているもの → 次にやる → その日の予定 → 近日の締め → その日の記録 → 立てる、の順に積み、日報のもとまで1画面で辿れるようにした |
| 予定の時間を日報に出すか | **出す（台帳には混ぜない）** | 会議に出た時間が日報から落ちると、その枠だけ空欄になって手で埋め直すことになる。台帳へは入れず、書き出し（`toPlainText`）が引数で予定を受け取って枠を取る。時間が決まっているぶん、枠取りはタスクより先 |
| 一覧の行に置く「始める」を言葉にするか | **記号（▶）だけにする（v1.18.1）** | 利用者の指摘「✓と始めるが混在している」。左に ✓ の丸、右に文字のボタンが並ぶと、押す物の大きさが揃わず行が2段に折れて、10件並べると画面が倍の長さになっていた。決定・取消を ✓ と ✕ にしたとき（v1.11.0）と同じ扱いにし、44×44 の記号に統一して `aria-label` と `title` に言葉を持たせた。予定の側（一時停止・終了）も、同じ行に並ぶので広い画面でも言葉を出さないようにした。**記録のカード（その日の記録）の「始める／止める」は言葉のまま**にしてある（あちらは幅のある面の主要操作で、記号だけにすると何のボタンか読めない） |
| 予定を入れる口を＋の扇にも置くか | **置く（v1.18.2）** | 利用者の指示。予定はカレンダーとスケジュールの中にしか入口が無く、そのために画面を移る必要があった。扇に5つ目として足し、**真上**（タスクの作り方4つの外側）に置いて色も分けた。いちばん指の届く左端はマイクに残してある（長押しのまま左へ滑らせて録音、が v1.12.1 からの手癖）。5つに増えたぶん間隔は 30°→22.5° に詰め、丸が重ならないよう半径を 136→172px（狭い端末は 116→150px）に広げた |
| 時刻の欄を空のまま押したときの既定 | **10:00 を入れてから選択画面を開く（v1.19.0）** | 利用者の指示。端末の時刻選択は「いまの時刻」から始まるので、朝でも夜でも業務時間まで回すことになっていた。空の欄を押した瞬間に 10:00 を入れる（DOM の値も同時に入れる。React の描画を待つと最初の1回だけ古い値で開く）。入れた時刻は ✕ で消せるので、**時刻なし（日付だけ）に戻す道は残る**。予定は時間の決まっているものがほとんどなので、作った時点で 10:00〜11:00 を下敷きにし、開始を動かしたら終了も追いかける |
| 予定の通知をどうするか | **タスクの期限と同じ仕組みに乗せる（v1.19.0）** | 利用者の指示。通知の仕組みを2つ持つと、出る条件も設定も二重になる。予定の開始時刻を「期限」と同じ扱いにし、札（tag）だけ `taskport-plan-` で分けた。設定のスイッチと「何分前」は共通で、出る条件（予約に対応した端末かどうか）も同じ。**プッシュサーバは持たない**方針は変えていない（予定を外へ送らない）。同じ分に2件重なったとき、先の1件を出した拍子にもう1件が「もう過ぎた」として落ちていたので、2分の猶予を持たせた |
| 通知のためにアプリ版（ストア配信）を作るか | **作らない。PWA のまま** | 予約つき通知（Notification Triggers）が使える端末なら、閉じていても出る。使えない端末で確実に出すには、外へ予定を送るプッシュサーバか、端末のアラームを叩くネイティブ層のどちらかが要る。前者は §3.2 の方針に反し、後者はストアの手続きと配信の仕組みを抱えることになって「直したその日に反映できる」（§10 ネイティブ vs PWA）を失う。実際の使い方では毎営業日アプリを開くので、取りこぼすのは「その日一度も開かなかった日」だけ。設定画面に、この端末で予約が使えるかどうかをそのまま出してある |
| アプリを開いている間、画面を消さないか | **既定で消さない（v1.19.0）** | 利用者の指示。現場では端末を置いたまま実行の画面を見て手を動かすので、そのたびに画面が消えると触り直しになる。電池を使うので設定で切れる。裏へ回すと端末側でロックが外れるため、戻ってきたら取り直す。**ロックは理由ごとに数える**（録音中とアプリぶんを別に数え、録音が終わった拍子にアプリぶんまで消えないようにする） |
| その日の区分の割合をどう出すか | **円グラフ（ドーナツ）＋名前と割合の一覧（v1.20.0）** | 利用者の指示。棒（区分ごとの時間）は「どれが多いか」は分かるが、「その日ぜんぶのうち何割か」が読めない。拾い方は日報と同じ（`worklog.ofDay`：完了した日・手を付けた日・その日が期限のもの）。**当初は実績が無いぶんを見込みで埋めていたが、v1.22.0 で実際に測った時間だけに改めた**（見込みが混じると稼働が実際と合わない）。**予定（打合せなど）の時間も足す** — 会議に出た時間が抜けると割合が実際と合わない。時間は主区分にだけ積む。切れは8つまでで、あふれたぶんは「その他」へ畳む（9つ目に新しい色を作らない） |
| 円グラフを色だけで見分けさせるか | **見分けさせない。名前と割合を必ず添える** | 区分の色（`--cat-*`）は「同じ区分はどこでも同じ色」を優先した配色で、色覚の型によっては隣り合う色の差が足りない組み合わせがある（藍と菫、緑と海松など。配色の検証で確認した）。色を変えるとチップ・絞り込み・棒グラフまで巻き込むので、**色は目印に留め、情報は数字で渡す**ことにした。切れのあいだに地の色の隙間を入れ、横の一覧に名前・割合・時間を出す。押すとその切れだけが残って他が薄くなる |
| 円グラフの出かた | **上から時計回りに一周描く（0→100%）** | 利用者の指示。切れは1つずつ「自分の開始位置から」伸び、開始の遅い切れほど遅れて出るので、全体では一筆書きに見える。仕組みは棒や折れ線と同じ `.tp-reveal`（画面に入った時に始まる）に乗せた。`prefers-reduced-motion` では動かさず最初から全部出す。押して目立たせる動きだけは待たせない（押してから間が空くと、押せていないように見える） |
| 始めたあとどこにいるか | **実行の画面のいちばん上へ移る（v1.20.1）** | 利用者の指示。一覧やカレンダーから ▶ を押しても押した場所に留まるので、いま何を数えているのかが見えなかった（画面のいちばん上が「いま動いているもの」）。**始めたときだけ**移り、止めたときは押した場所に留まる（続けて別のものを止めるため）。画面が変わるときは一気に、同じ画面にいるときは滑らせて上へ戻す。予定の開始・再開、区分から立てて始めたときも同じ |
| 止めた仕事にどう戻るか | **「止めてあるもの」の面を作る（v1.21.0）** | 利用者の指示。止めると一覧の中に紛れ、どこまでやったか・どれを再開するかが分からなくなっていた。動いているものの直下に橙で並べ、▶ で止めたところから続きを数える。同じものが2か所に出ないよう、「次にやる」からは外す。予定も同じ面に並べる（止めてある実行ログ） |
| 1分に満たない実行の扱い | **20秒未満は捨て、20秒〜1分は1分として数える** | 触った印（`actualMin`）が残らないと「止めてあるもの」に出ず、再開する道が消える。かといって押し間違いまで数えると日報が汚れる。境目を20秒に置いた。最小単位を1分にするのは日報の書き方に合わせたもの |
| 実行の面に完了したものを残すか | **残さない。件数だけ出して畳む（v1.21.0）** | 利用者の指示。済んだ仕事が積み上がると、いま手を動かすものが埋もれる。既定では未完了だけを出し、「完了した◯件」を押したときだけ開く。**消すのではなく畳む**のは、完了したあとに実績（開始時刻・かかった時間）を直すことがあるため |
| 画面を切り替えたときの位置 | **必ずいちばん上から出す（v1.21.0）** | 利用者の指示。前の画面のスクロール位置が残っていて、開いた先の途中から始まって見えることがあった |
| メニューの並び | **実行を最上段にする（v1.21.0）** | 利用者の指示。1日のうちいちばん開くのが実行の画面で、一覧はその次 |
| 本日の稼働を何で数えるか | **実際に測った時間だけ（v1.22.0）** | 利用者の指摘「本日の稼働が実際のものと違う」。見込み（`estimateMin`）で埋めていたので、まだ手を付けていない仕事のぶんまで稼働に乗っていた。数えるのは**押して測ったもの**（実行ログの区間と、人が入れた `actualMin`）だけにし、測っていない仕事は件数だけを注記に出す（`measuredOfDay`）。円グラフも同じ数え方に揃えた（数字が2つあると、どちらが本当か分からなくなる） |
| タスクの実行の区間をどこに残すか | **実行ログにも残す（合計は台帳のまま）（v1.22.0）** | 「いつ何をしていたか」は `startedAt` だけでは残らない（止めた時点で消える印なので、その日2回に分けた仕事は最後の1回しか場所が分からない）。区間は予定と同じ実行ログ（`WorkRun.segments`）へ、合計は台帳（`actualMin`）へ置く。**実績が2か所に増えたわけではない** — 台帳の値が正で、実行ログは「押した時刻の記録」。人が直したときも実行ログは書き換えない |
| 何時に何をしていたかをどう見せるか | **勤務時間を1本の帯にして色で置く（v1.22.0）** | 利用者の指示。円グラフは「何にどれだけ」は分かるが「いつ」が分からない。始業から終業までを幅いっぱいの帯にし、実行ログの区間をそのまま置く。予定は斜線を重ねてタスクと見分ける。**色だけで伝えない**方針は同じで、幅のある帯には件名を書き、押せば中身が読める。1分に満たない区間も1分ぶんの幅で置く（合計に入っているのに帯から消えると食い違う） |
| 見出しの言葉（2回目） | **カタカナ、無ければ漢字に戻す（v1.26.0）** | 利用者の指示。v1.24.0 で英語（RUNNING / UP NEXT …）にしたが、実際に使うと**目が英語を読みに行く**ぶん遅かった。カタカナで通じる語はカタカナ（スケジュール・カレンダー・タスク）、無ければ漢字（実行中・停止中・次の作業・近日締切・時刻未定・週間予定・工数）にした。面の切れ目が読めるかどうかは、英語かどうかではなく**字面が本文と違うか**なので、短い名詞で足りる |
| 扇の丸の大きさと名前の場所 | **＋と同じ 60px にして、名前を丸の中へ（v1.26.0）** | 利用者の指示。名前を丸の外（下）に出していたぶん、隣の丸と重ならないよう丸を 46px まで小さくしていた。名前を中に入れると重なりようがないので、丸を ＋ と同じ大きさに戻せる（押し外しも減る）。字は 9.5px で、アイコンの下に置く |
| 「今日へ」のボタン | **外す（v1.26.0）** | 利用者の指示。日付の送り（＜ ＞）で戻れるうえ、画面を開き直せば今日から始まる。押す物が1つ減る |
| 先の日の「やったことを足す」 | **出さない（v1.26.0）** | 利用者の指示。まだ起きていない仕事を「やったこと」として入れる道は要らない（実績が未来に立つと稼働も日報も狂う） |
| スケジュールの日の操作 | **記号だけにする。稼働の札は外す（v1.26.0）** | 利用者の指示。「予定を入れる」「タスクを作る」の文字が見出しの行を押し広げ、日付が小さく見えていた。v1.18.1（一覧の行）と同じ扱いで 44×44 の記号にし、言葉は読み上げと長押しに持たせた。「3時間20分 / 8時間」は分析の本日の稼働と同じ数字なので、ここには出さない |
| 時刻を決めていない仕事の置き方 | **空いている時間へ1押しで置く（v1.29.0）** | 利用者の指示（タイムボクシングの紹介を参考に）。台帳に「やること」はあっても、**いつやるか**を決める道が編集画面で時刻を手入力するしかなかった。DAY の「時刻未定」の各行に置き先の時刻を出し、押すとその時刻（`dueTime`）が入って軸に並ぶ。置き先は勤務時間から休憩・予定・すでに時刻の決まった仕事を引いた**空き**の、いちばん早いところ。今日はいまより前には置かない。入りきらないものは「空きなし」と出して詰めない（黙って押し込むと、その日の計画が最初から嘘になる）。計算は `lib/timebox.ts` の `freeSlots` / `firstFit` |
| 置くのに使う時間 | **見込み（`estimateMin`）だけ（v1.29.0）** | 実績（`actualMin`）は「かかった時間」なので、これから置く長さには使わない。空きの合計も見込みで出すが、**稼働の集計には混ぜない**（数えるのは押して測った時間だけ＝v1.22.0）。画面にも「見込みの長さぶん」と書く |
| よくやる業務をどう出すか | **実行の画面に上位3件、1押しで始まる（v1.31.0）** | 利用者の指示。同じ仕事は何度も回ってくるのに、そのたびに台帳から探すか1件立て直していた。**押して動かした記録**（分析と同じ `worklog.entriesInRange`）から拾い、**やった日の数**の多い順に並べる（同じ日に5回押した1件より、5日にわたって1回ずつやった1件のほうが「よくやる」に近い）。行の作りは「次の作業」と同じ（▶ が左・件名・下に小さく数字）で、違うのは行ぜんぶが押しどころなところ。3件までにするのは、並べすぎると「次の作業」が下がって、いま締めるべきものが見えなくなるため |
| よくやる業務を押したとき何をするか | **同じ件名の未完了があればそれを始め、無ければ1件立てて始める（v1.31.0）** | 押すたびに新しい1件を立てると、同じ件名が台帳に積み上がって、どれを完了にすればよいか分からなくなる。**見込み（`estimateMin`）には何も入れない** — 過去の実績の平均を見込みの欄に入れると実績と見込みが混ざる（CLAUDE.md §3.3）。平均は行に**実績として**出すだけにした。動かしている最中のものはこの面に出さない（押すと止まってしまう） |
| よくやる業務をどこに置くか（2回目） | **実行の画面から外し、右下の ＋ の上に5件（v1.32.0）** | 利用者の指示。実行の画面に並べると「次の作業」が下がり、いま締めるべきものが見えなくなっていた。＋はどの画面にも出ているので、押したい仕事を探しに画面を移らなくてよい。丸のアイコンでは件名が読めないので、扇の丸とは形を分けて**名前の見える札**にし、扇の2層目より上に置く。下から順に「よくやる順」＝いちばん押すものが親指のいちばん近くに来る。扇と同じで長押しのまま滑らせて選べる。5件までにするのは、これ以上並べると札が画面の上へはみ出し、扇の丸まで指が届かなくなるため |
| DAY の日付帯をどこまで動かせるか | **◀▶ で1週間ずつ、前へも先へも限りなく（v1.32.0）** | 利用者の指摘「過去スケジュールの DAY が今週以前にいけない」。帯を今日に貼り付けていたので、カレンダー（MONTH）から過去の日を選べても、その日は帯に無く、そこから前後の日へ動けなかった。帯の左端を状態として持ち、帯の外の日を選んだときはその日が入るところまで帯ごと動かす。選んでいる日の札は帯の中央へ寄せる（帯が動いたことが分からないため）。**「今日へ」の釦は足さない** — v1.26.0 の「外す」（利用者の指示）を戻さない。日付の送りは ◀▶ が持つ |
| ポップアップ（シート）の出しかた | **地をぼかした全画面の上に、真ん中の1枚（v1.33.0）** | 利用者の指示。画面の下辺に貼り付く形をやめた。下に貼り付いていたときは、**キーボードが出ると入力欄も決める釦もキーボードの下に潜り**、どこに何を打っているのか見えなかった。真ん中に浮かせ、面の高さを**いま見えている範囲**（`visualViewport`。`lib/viewport.ts` が `--vv-h` / `--vv-t` を置く）に合わせる。見出しと下の段は動かさず、中身だけが送られるので、キーボードが出ていても「何を打っているか」と「決める・やめる」が同時に見える。対応していない端末では 100dvh で従来どおり。地をぼかす面は `position: fixed` の基準になるので、この中から開く面（`CategorySheet`）は `createPortal` で body へ出す |
| よくやる業務をどこに出すか（3回目） | **画面の真ん中あたり。時間は出さない（v1.33.0）** | 利用者の指示。＋の真上に積んでいたときは、いちばん読みたい項目の名前が画面のいちばん端にあった。扇の丸が要る場所を下の余白として空けたうえで、残りの真ん中に置く（重なることはない）。平均時間は押す前に読むものではないので落とし、**何をやるか（項目の名前）だけ**にした |
| 分析をどの単位で見せるか | **日・週・月・全体を上で切り替える（v1.30.0）** | 利用者の指示。面ごとに期間がばらばら（円グラフはその日、区分ごとの時間は直近10日、推移は90日）で、同じ画面の数字どうしを足し引きできなかった。切り替えと前後の送りを**いちばん上に貼り付け**、どの面も同じ期間で描く。過去も同じ道で辿れる |
| 分析の面の順番 | **区分の割合 → 時間帯 → 区分ごとの時間 → 推移 → その他（v1.30.0）** | 利用者の指示。そのあとに 稼働／作業ごとの工数／案件ごとの工数／見込みと実績／曜日ごと を置いた。面が増えたときは保存された並びを既定へ戻す（後ろへ足すだけだと、増えた面がいつも最後に付き、並びを決め直した意味が消える） |
| 時間帯を横帯と縦軸のどちらにするか | **縦軸（v1.30.0）** | 利用者の指示。横帯だと1日ぶんが画面の幅に押し込まれ、10分の仕事は線1本になって件名も出せなかった。スケジュールの DAY と同じ軸（`TimelineAxis`）を使い、2つの画面で同じ日が同じ形に見えるようにした。週・月・全体は日をまたいで帯にならないので、**縦軸はそのまま時刻**にして横棒で合計を出す（`HourBars`） |
| スケジュールに実績を出すか | **同じ軸に出す（v1.30.0）** | 利用者の指示「実際に行ったタスクも随時入れていく」。予定だけを並べても、その通りに動かない日のほうが多い。押して測った区間とあとから足した記録を ✓ の付いた面として同じ軸に置き、予定と横に並べた。押すとその場で実績（開始時刻・かかった時間）を直せる |
| 分析の数え方をどこに置くか | **`lib/analysis.ts` に集約（v1.30.0）** | 面ごとに拾い方が違うと、同じ画面で数字が食い違う。素は `worklog.entriesInRange`（実行の記録＋あとから足した記録）ひとつだけにして、円グラフ・時間帯・区分ごとの時間・工数・書き出しをすべてそこから出す。人が直した実績（`actualMin`）があるときは**そちらを正**として合計を寄せるが、**区間そのものは動かさない**（区間は「実際に押した時刻」の記録） |
| 工数の画面を残すか | **やめて分析へ入れる（v1.30.0）** | 利用者の指示「工数の専用ページは不要」。案件ごとの時間は期間で見るものなので、期間の切り替えを持つ分析に置くほうが素直で、画面も1つ減る。作る・直す（`JobSheet`）と、案件に入れていない仕事の割り当てはその面に残した。`?view=jobs` は分析へ送る |
| 実績からの工数をどう出すか | **作業ごとの CSV を足す（v1.30.0）** | 利用者の指示「外部出力時に実績からどの作業にどれだけ工数がかかっているか計算したい」。件名×区分×案件でまとめ、時間（0.1時間刻み）・分・回数・日数を出す。期間は書き出しの画面で選ぶ。**画面と同じ数え方**にしてあるので、CSV と分析の数字が突き合わせられる |
| 完了にしたとき実行の記録をどうするか | **同じ時刻で閉じる（v1.30.0）** | 利用者の指摘「分析のスケジュールタスクが実際と異なる」。動かしたまま完了にすると区間が開いたままで、「いまも動いている」ものとして数えられ、時間帯が深夜まで伸びて合計が24時間を超えていた。完了で閉じ、過ぎた日に開いたままのものは**起動時にその日の終業で閉じる**（`staleRuns`）。閉じたことは画面に出す（黙って数字を書き換えない） |
| 実行の画面に何を置くか | **「いま手を動かすもの」だけにする（v1.28.0）** | 利用者の指示。やったことを足す・本日の記録・区分から開始の3つを外した。区分から開始は右下の ＋ の扇に同じ道があり、やったことを足すと実績直しは**その日を見ている画面**（スケジュールの DAY・分析のその日）のほうが、入れた結果がその場で見える。同じ道を2か所に置かない、という決まりに揃えたもの |
| やったことを足す の置き場 | **スケジュール（DAY）と分析（その日）（v1.28.0）** | 上と同じ指示。画面の中から開くのでシート（`AddLogSheet`）にして body へ出す。**先の日には出さない**（v1.26.0 の決まりはそのまま。呼ぶ側が day を確かめる）。分析は台帳が空のときでも足せるよう、空の画面にも釦を置く |
| 本日の記録（実績を直す面） | **外す。直すのは分析から（v1.28.0）** | 利用者の指示。実績を直す道は分析の円グラフ・時間帯から開く `ActualSheet` にある（v1.22.0）。実行の画面に同じ表を置くと、日報の数字を触る場所が2つになる |
| 朝の仕分け・明日の準備の見た目 | **札を外し、灰色（無効）もやめる（v1.28.0）** | 利用者の指摘「グレーありとなしがある」。期限超過が0件のとき片方だけ押せない灰色になり、件数の札の有無で大きさも揃わなかった。2つとも同じ見た目にして、中身が無いときはその画面が「ありません」と言う |
| 次の作業の1件 | **▶ だけを左に置き、✓ の丸は外す（v1.28.0）** | 利用者の指示。この面は「次に手を付けるもの」を選ぶ場所で、済ませる・やめるは行を押した先（編集画面）にある。押す物が1つになるぶん行を1枚のカードにして、件名を大きく、期限・見込み・区分を下の行に小さく置いた。近日締切も同じ形に揃えた（▶ の位置が面ごとに違うと壊れて見える） |
| DAY の見出しの並び（2回目） | **左上に名前・右上に日付（v1.27.1）** | 利用者の指示。ほかの面（WEEK / MONTH）も左に名前・右に件数の札なので、DAY だけ逆だと目の動きがそこで止まる。名前の位置を揃え、日付は右の札の場所へ移した |
| 縦軸の1件に枠を入れるか | **四方に枠を入れる（v1.27.1）** | 利用者の指摘「スケジュール毎の枠がない」。地の色だけで置いていたので、続けて入っている同じ区分の2件（8:30〜10:00 と 10:00〜11:00）が1枚の面に見え、切れ目が読めなかった。区分の色を薄めた 1px の枠で囲み、左端の 3px はそのまま残す（枠は `border-box` なので高さは変わらない） |
| 予定とタスクの見分け | **予定は斜線を入れた色面にする（v1.27.1）** | 利用者の指示。色は区分のものを使うので、色だけではタスクと予定の別が付かない。**タスク＝塗り、予定＝斜線**にして、地の作りで見分けられるようにした（色覚に頼らない）。Googleカレンダーの予定は区分を持たないので、これまでどおりブランドの色で細い斜線 |
| スケジュールの面の名前 | **DAY / WEEK / MONTH の英語1語（v1.27.0）** | 利用者の指示。見出しを日本語へ戻した v1.26.0 の例外を、この3つだけに置く。ここで要るのは「いま見ているのが日か週か月か」の別だけで、意味を読ませる言葉が要らない。3語とも一目で長さが違うので、字を読まずに面の切れ目が分かる。ほかの面（期限超過など）は日本語のまま |
| スケジュールの日の操作（2回目） | **面の中の釦を外し、右下の ＋ に一本化（v1.27.0）** | 利用者の指示「予定追加ボタンを廃止（＋からできる）」。v1.26.0 で記号だけにした2つ（予定を入れる・タスクを作る）も、同じことが ＋ の扇からできる。入口が2か所にあると、どちらから入れたかで結果が違うのではないかと考えることになる。空いた見出しの行は、**左上に日付・右上に面の名前**だけにした |
| 予定の1行の書き方 | **件名＋所要時間の1行（「所要確認 15min」）（v1.27.0）** | 利用者の指示。時刻（10:00–10:15）と件名を2行に積んでいたので、15分の予定では枠に2行が入らず、**件名が切れて読めなかった**（何の予定か分からない枠が並ぶ）。始まりの時刻は軸の位置が示すので、行に置くのは件名と所要時間だけにする。時間は薄く小さい字（`--faint` の 10px）で、件名の桁を食わない。時刻は読み上げ（`aria-label`）と長押しの吹き出しに残す |
| 消す操作の置き場と確かめ方 | **下の段の左端に固定し、押したら窓を1枚はさむ（v1.26.0）** | 利用者の指示。面の中に「このタスクを消す」を埋めていたので、長い編集画面ではスクロールしないと見つからず、その場で開く確認も本文の一部に見えていた。決める操作（✕ ✓）と同じ段に置き、**左端へ離して**取り違えを防ぐ。押すと `ConfirmDialog` が中央に出て、OK を押したときだけ消える。タスク・予定・案件で同じ形にした |
| 工数をどの単位で持つか | **案件（Job）を足す。区分とは別の軸（v1.25.0）** | 利用者の指示「工数管理もできるようにする」。区分（業務分類）だけでは「何をしたか」しか出ず、「どの案件にどれだけ使ったか」が出せなかった。タスクと予定に `jobId` を1つ持たせ、案件の側は名前・管理番号・相手先・見積工数・期限だけを持つ。**区分を置き換えない**（日報は区分で書くので、両方が要る） |
| 工数の実績を何で数えるか | **押して測った時間だけ（v1.25.0）** | 稼働・円グラフと同じ数え方に揃えた（v1.22.0）。見積（案件の予算）と実績を1つの数字に混ぜると、どちらを見ているか分からなくなる。画面では「実績 / 見積」を並べ、残り見込み（未完了タスクの見積）はさらに別の欄に置く。期間（今週・今月）は実行ログから出すので、**90日より古いぶんは出ない**ことを画面に書く |
| 短い区間を工数でどう扱うか | **秒で足してから分にする（v1.25.0）** | 1件ずつ分に丸めると、20〜59秒の区間が全部 0分になり、台帳側の数え方（20秒以上は1分＝v1.21.0）と食い違った。案件ごとに秒で積んでから1回だけ分にする |
| 案件をどこに保存するか | **また別のDB（`taskport-jobs`）** | 予定のDBに足すとそちらの版を上げることになり、古い版を掴んだタブがあるだけでアップグレード待ちで固まる（v1.2.0 の不具合）。DBを足すぶんには何も起きない |
| 案件を消したときのタスク | **タスクと予定は消さず、案件なしに戻す** | 案件を畳んでも、その時間に働いた事実は変わらない。実績（`actualMin`）はタスクに残り、日報と稼働はそのまま |
| 一覧の画面を残すか | **やめて実行へ集約する（v1.24.0）** | 利用者の指示。「いま動かす」と「台帳から探す」で画面を行き来していたが、実際には同じ流れの中で起きる（次にやるものを決める→台帳で探す→始める）。実行の画面のいちばん下に TASKS（検索・絞り込み・今日/今週/すべて/完了）を置き、朝の仕分けと明日の準備も一緒に移した。無くなった `?view=list` は実行へ送る |
| カレンダーの画面を残すか | **やめてスケジュールへ集約する（v1.24.0）** | 利用者の指示。同じ日を2つの画面で開けると、どちらで直したか分からなくなる。月の升目は `MonthGrid` として切り出し、スケジュールのいちばん下に置いた。升目の日を押すと、上の「その日」がその日に変わる |
| その日をどう見せるか | **縦軸の時間の並びに戻す（v1.24.0）** | 利用者の指摘「その日のスケジュールが分かりにくい」。v1.16.0 で2時間の枠（午前前半…）に畳んだが、枠に畳むと**何時から何時まで空いているか**が読めず、枠の切れ目をまたぐ予定の置き場も決められなかった。始業から終業までを1本の軸にし、重なった時間は横に並べる（時間帯の帯と同じ `packColumns`）。畳んだ形の利点だった「時刻を決めていない仕事が見えなくなる」問題は、軸の下に **NO TIME** の一覧を置いて残した。**完了したものはここに出さない**（選べないものを並べても場所を食うだけ） |
| 1週間をどう出すか | **予定のある日だけ並べる（v1.24.0）** | 利用者の指示。2週間ぶんを全部並べていたが、予定の無い日は「—」が続くだけで、読むところが無かった。予定のある日だけを出せば、1画面で「今週どこが埋まっているか」が読める |
| 実行の画面から何を外すか | **SPENT と「その日の予定」を外す（v1.24.0）** | 利用者の指示。SPENT（その日の積み上がり）は分析の TODAY と同じ数字で、2か所に出す意味が無かった。その日の予定はスケジュールで見て直すものなので、実行の画面には置かない。**RUNNING は1件も動いていなければ面ごと出さない**（空の枠が居座ると UP NEXT が下がるだけ） |
| 見出しの言葉 | **英語にする（v1.24.0）** | 利用者の指示。RUNNING／UP NEXT／TASKS のように、面の名前は短い英語のほうが視線の止まる場所として効く（日本語だと本文と同じ見た目になり、面の切れ目が読めない）。**説明とボタンの言葉は日本語のまま**（CLAUDE.md §4.3 の「動作をそのまま書く」は崩さない） |
| 時間帯を円グラフの中に置くか | **独立したカードにして、本日の稼働の下に置く（v1.23.0）** | 利用者の指示。割合の面の付け足しになっていて、いちばん見たい「何時に何をしていたか」が下のほうにあった。カードを分けて2枚目に上げ、**押すとポップアップ**（`BandSheet`）で開くようにした。狭い端末では帯そのものに件名が入りきらないので、ポップアップでは「何時〜何時・何を・どれだけ」を一覧にし、行を開くとその場で実績（開始時刻・かかった時間）を直せる |
| 分析の面の並びを誰が決めるか | **設定が持ち、画面から並べ替える（v1.23.0）** | 利用者の指示。何をいちばん上に置きたいかは人と時期で変わる。コードが持つのは「どんなカードがあるか」と既定の並びだけにし、実体は `Settings.dashOrder` に置いた。並べ替えは ↑↓ の2つのボタンにしてある（掴んで動かす仕組みは、指では取りこぼしが多く、キーボードからも触れなくなる） |
| ヘッダー右上に何を置くか | **実行中の印。書き出しは引き出しへ一本化（v1.23.0）** | 利用者の指示。書き出しは引き出しにも同じものがあり、同じ道を2つ持っていた。空いた場所に「いま動かしているもの」の印を置き、押すと実行の画面へ行く。色は動かしている区分のもの、**2つ以上のときは本数を数字で出す**（色だけで伝えない）。止め忘れに、どの画面にいても気づける |
| 時刻の欄が「10:0」と切れる | **日付と時刻を同じ行に3つ並べない（v1.23.0）** | 利用者の指摘。端末の時刻入力は中身を縮めてくれないので、3列に割ると数字が切れる。560px より狭いときは日付が1行を使い、時刻は2つで分け合う形にした。あわせて欄の左右の余白と ✕ を詰めてある |
| 時間帯の帯で、同時に動かしたぶんをどう置くか | **行を増やして並べる（v1.22.1）** | 利用者の指示。電話を受けながら伝票を打つ、が実際に起きる（実行の画面が同時に何本でも動かせるのはそのため）。1本の帯に重ねて置くと、下になったほうが読めず押せない。早く始まったものから**空いている中でいちばん上の行**へ置き、終わった行は次の区間で使い回す（`packLanes`）。行の高さは 30px で、行が増えたぶんだけ帯が縦に伸びる |
| 期限の無い仕事を、その日の集計にどう残すか | **実行ログから拾う（v1.22.1）** | その日に出すかどうかは 完了日／`startedAt`／期限 で決めていたが、`startedAt` は止めた時点で消える印なので、**期限を入れずに始めて止めた仕事**がその日の稼働・円グラフ・記録・日報から丸ごと落ちていた。`ofDay` に実行ログを渡し、「その日に押して動かしたもの」も同じ日の記録として拾うようにした（台帳に日付の項目は足していない） |
| 実績を直す道をどこに置くか | **円グラフの区分と、時間帯の帯から直接開く（v1.22.0）** | 利用者の指示。それまでは実行の画面の「その日の記録」まで戻る必要があり、グラフで「この区分が多すぎる」と気づいた場所から直せなかった。押した先で開くのは**かかった時間と開始時刻の2つだけ**（件名や期限はカードから）。よく使う長さ（15〜120分）を押すだけで入れられる。この面は画面の中から開くので、body へ出して描く（`.tp-main` が重なりの基準になっていて、その中では ＋ ボタンの下に潜る） |
| 実行中のものをどう目立たせるか | **枠を2秒周期で点滅させる（v1.22.0）** | 利用者の指示。止めてあるものと並ぶと、どれが今動いているかが色の違いだけになっていた。動きは枠の色と影だけ（中身は動かさない。読んでいる最中に文字が動くと読みにくい）。予定は予定の色で点滅させる。`prefers-reduced-motion` では止める |
| 「区分から始める」と「タスクを始める」を同じ画面に入れるか | **入れない。別々に開く（v1.22.0）** | 利用者の指示。1つの画面のタブにしていたので、扇で「区分」を選んでも中でもう一度タブを押すことがあった。v1.13.0 で作る系に対して決めたこと（扇で選んだものがそのまま開く）と同じ扱いに揃えた |
| 扇の7つをどう並べるか | **2層にする（v1.22.0）** | 利用者の指示。7つを1つの弧に並べると間隔が 15° まで詰まり、隣を押しやすかった。**＋に近い内側（120px）に「いまの日の操作」（区分・始める・予定）、外側（216px）に「あとでやることを作る」（手描き・記憶・文章・マイク）**を置いた。層で役目が分かれるので、間隔は 30° に戻せている。左端のマイクは 180° のままで、長押しのまま左へ滑らせる手癖（v1.12.1）を崩していない |
| 実行を終えたらタスクも完了にするか | **しない** | 手順を全部済にしてもタスクを完了にしないのと同じ理由（CLAUDE.md §9）。「時間を数え終えた」と「その仕事が終わった」は別。終了を押したときに「完了にするときは丸を押してください」とだけ添える |

---

## 11. 用語

| 語 | 意味 |
|---|---|
| Port In | 入口。タスクがシステムに入ってくる経路 |
| Port Out | 出口。タスクが別の形で取り出される経路 |
| Draft | AIが提案した未保存のタスク候補 |
| 構造化パイプライン | 自然文を Draft 配列に変換する共通処理 |
