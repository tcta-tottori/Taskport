# Taskport 設計書

> タスクとスケジュールを、どの端末からでも・どんな方法でも入れられて、
> 必要な形で出せるようにするための個人用ハブ。

- **リポジトリ名**: `taskport`
- **公開URL**: `https://tcta-tottori.github.io/Taskport/`
- **バージョン**: v1.0（初版）
- **作成日**: 2026-08-18

---

## 1. 背景と目的

### 1.1 現状の課題

生産管理部の業務では、タスクが発生する瞬間と、それを記録できる瞬間がずれる。

- 現場や倉庫を歩いている最中に「あれを確認しないと」と気づくが、その場でPCは開けない
- 会議・電話の直後に3件も4件も宿題が積まれるが、メモを取る前に次の用件が来る
- メールやチャットで来た依頼が、受信箱に埋もれてタスク化されない
- 結果として、期限と優先順位が頭の中にしか存在しない状態になる

### 1.2 このアプリが解決すること

**入口を増やして、記録のコストをゼロに近づける。**
歩きながらなら喋る。PCの前ならキーボード。メールから来た依頼なら共有ボタン。
入口が何であれ、行き先は一つの場所にまとまる。

**出口を増やして、記録が死蔵在庫にならないようにする。**
一覧で見る、日付軸で見る、カレンダーに送る、日報用に書き出す。
入れた情報が、使いたい形で取り出せる。

### 1.3 名前の由来

**port = 入出力ポート**。多数の入口と出口を持ち、そのすべてが同じ一つの場所につながっている、という構造をそのまま名前にした。「Voice」を名前に入れないのは意図的で、音声は数ある入口の一つに過ぎない。

---

## 2. スコープ

### 2.1 やること

| 区分 | 内容 |
|---|---|
| 入力 | 音声・キーボード・フォーム直接・他アプリからの共有 |
| 構造化 | 自然文をAIがタスク候補に分解し、**人が確認・修正してから**登録 |
| 管理 | 期限・優先度・区分・メモ・完了状態 |
| 出力 | 一覧／スケジュール／カレンダー連携／テキスト書き出し／CSV |
| 動作環境 | Android（主）・PC（副）。PWAとしてホーム画面に追加 |

### 2.2 やらないこと（初版）

- チーム機能（担当者アサイン、コメント）→ Phase 3 で検討
- ガントチャート、工数管理、進捗率
- 社内基幹システムとの直接連携
- ネイティブアプリ化（ストア配信）

### 2.3 前提

- 主な利用者は1名（Phase 1〜2）
- 利用ブラウザは **Chrome**（Android / Windows）。音声入力は Chrome 前提とし、非対応環境ではキーボード入力に自動フォールバック
- 業務情報（取引先名・品番・数量）を扱うため、セキュリティ要件は §8 に従う

---

## 3. 設計思想

### 3.1 中核となる比喩

```
        入口（Ports In）                            出口（Ports Out）
   ┌──────────────────┐                      ┌──────────────────┐
   │ 音声              │                      │ 一覧ビュー        │
   │ キーボード（自然文）│                      │ スケジュールビュー │
   │ フォーム（直接）   │  ──▶  [ Taskport ] ──▶ │ Googleカレンダー  │
   │ 他アプリから共有   │        単一の台帳      │ テキスト書き出し   │
   │ カレンダー取込     │                      │ CSV / スプレッドシート│
   └──────────────────┘                      └──────────────────┘
```

**設計上の含意**: 入口と出口は将来必ず増える。したがってコードは、
入口を「自然文 → タスク候補」の共通パイプラインに集約し、
出口を「タスク配列 → 任意の形式」の変換器として独立させる。
入口を1つ足すのに本体を触らずに済む構造にすること。

### 3.2 三つの原則

**原則1 — AIは提案し、人が決める**
自然文の解釈は必ず確認画面を挟む。全自動登録はしない。誤った期限が黙って入る方が、確認の一手間より遥かに高くつく。

**原則2 — 音声は端末から出さない**
音声認識はブラウザ内蔵の Web Speech API で端末側処理する。外部に送るのは認識後のテキストのみ。取引先名や数量を含む音声データそのものをクラウドに置かない。

**原則3 — 保存層は差し替え可能にしておく**
Phase 1 は端末内、Phase 3 は共有ストレージ。ここを最初からインターフェースで分離しておけば、共有化の際に画面側を書き直さずに済む。

---

## 4. アーキテクチャ

### 4.1 構成図

```
┌──────────────────────────────────────────────────┐
│  ブラウザ（Android Chrome / PC Chrome）             │
│                                                  │
│   音声 ──▶ Web Speech API ──▶ テキスト             │
│   録音 ──▶ MediaRecorder  ──▶ 音声（端末内のみ）    │
│                    │                             │
│              ┌─────▼──────┐                      │
│              │ parseToTasks│ 端末内で解析          │
│              └─────┬──────┘ 外部へ送らない         │
│                    ▼                             │
│              ┌───────────┐                       │
│              │ ReviewSheet│ 必ず人が確認           │
│              └─────┬─────┘                       │
│  ┌──────────┐      ▼                             │
│  │  UI層     │ ┌────────────┐  ┌──────────────┐   │
│  │  React    │─│ Repository │─▶│  IndexedDB   │   │
│  └──────────┘ │ （抽象）    │  │ taskport      │   │
│               └────────────┘  │ taskport-media│   │
│                               └──────────────┘   │
└───────────────────────┬──────────────────────────┘
                        │ 利用者が選んで押したときだけ
                        ▼
              ┌──────────────────────┐
              │ Google Calendar API   │  予定の読込・追加
              └──────────────────────┘
```

### 4.2 技術選定

| 層 | 採用 | 理由 |
|---|---|---|
| ビルド | Vite | 軽量・高速。GitHub Pages と相性が良い |
| UI | React + TypeScript | 型でタスク構造を固定でき、入出力追加時の事故を防ぐ |
| PWA | vite-plugin-pwa | オフライン動作とホーム画面追加を標準機能で |
| 保存 | IndexedDB（`idb` ラッパ） | 容量制限が緩く、オフラインで確実に残る |
| 音声 | Web Speech API | 無料・端末内処理・Android Chrome で日本語精度が高い |
| 解析 | 端末内の自前ロジック | 外部サービスを使わない（§10.1）。文章が端末から出ない |
| ホスティング | GitHub Pages | 無料・`onshitsu` と同じ運用で学習コストゼロ |
| カレンダー | Google Identity Services + Calendar API | 予定の読込と追加。クライアントIDは利用者が用意する |

### 4.3 ディレクトリ構成

```
taskport/
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── types.ts                  # Task / Draft / 列挙型の定義
│   ├── ports/
│   │   ├── in/
│   │   │   ├── useVoiceInput.ts      # 音声 → テキスト
│   │   │   ├── useTextInput.ts       # 自然文メモ → テキスト
│   │   │   ├── useShareTarget.ts     # 他アプリからの共有受け取り
│   │   │   └── parseToTasks.ts       # テキスト → タスク候補（共通パイプライン）
│   │   └── out/
│   │       ├── toCalendar.ts         # Googleカレンダー用URL / ICS生成
│   │       ├── toPlainText.ts        # 日報・朝会用テキスト
│   │       └── toCsv.ts              # CSV書き出し
│   ├── repository/
│   │   ├── Repository.ts         # インターフェース定義
│   │   ├── LocalRepository.ts    # Phase 1: IndexedDB
│   │   └── SheetRepository.ts    # Phase 3: GAS + スプレッドシート
│   ├── views/
│   │   ├── ListView.tsx
│   │   ├── ScheduleView.tsx
│   │   ├── ReviewSheet.tsx       # AI候補の確認・修正
│   │   ├── TaskEditor.tsx
│   │   └── ExportSheet.tsx
│   ├── components/
│   │   ├── TaskCard.tsx
│   │   ├── InputDock.tsx         # 画面下部の入力ドック
│   │   └── Segmented.tsx
│   └── styles/
│       └── tokens.css            # 色・タイポのトークン
├── public/
│   ├── manifest.webmanifest
│   └── icons/
├── design.md
├── CLAUDE.md
└── README.md
```

---

## 5. データモデル

### 5.1 Task

```ts
type Priority = "high" | "mid" | "low";
type Status   = "open" | "done";
type Source   = "voice" | "text" | "form" | "share" | "calendar";

interface Task {
  id: string;            // ULID（時系列にソート可能なID）
  title: string;         // 「〜する」で終わる実行形
  note: string;          // 相手先・数量・背景などの補足
  due: string | null;    // "YYYY-MM-DD"。期限なしは null
  dueTime: string | null;// "HH:mm"。時刻指定がある予定のみ
  estimateMin: number | null; // 見込み所要時間（分）。未見積は null
  priority: Priority;
  category: string;      // 発注 / 納期確認 / 在庫 / 社内資料 / 会議 / 通関 など自由入力
  status: Status;
  source: Source;        // どの入口から入ったか（改善のため記録）
  createdAt: string;     // ISO 8601
  updatedAt: string;
  doneAt: string | null;
}
```

**設計上の注記**

- `priority` は内部的に英字。表示は 高／中／低。将来の多言語化と、CSVでの並べ替えを考慮
- `category` は自由入力。ただし候補は業務日報の作業内容から取ったマスタを持つ（§10.1、`src/lib/workCategories.ts`）。マスタ外の語も書けて、その場合は「その他」として集計する
- `source` を持つのは、「どの入口が実際に使われているか」を後から見て、育てる入口を判断するため
- 削除は物理削除とする。個人用途で論理削除の複雑さに見合う利点がない
- `estimateMin` は勤務時間に対する積み上げ量の計算とスケジュールの帯の長さに使う。
  null（未見積）は Settings の `defaultEstimateMin` で埋めて計算する（§10 参照）

### 5.2 Draft（AI候補・未保存）

```ts
interface Draft extends Omit<Task, "id"|"status"|"createdAt"|"updatedAt"|"doneAt"> {
  tempId: string;
}
```

確認画面でのみ存在し、登録時に `Task` へ変換される。

### 5.3 Repository インターフェース

```ts
interface Repository {
  list(): Promise<Task[]>;
  add(tasks: Task[]): Promise<void>;
  update(id: string, patch: Partial<Task>): Promise<void>;
  remove(id: string): Promise<void>;
}
```

画面側は **必ずこのインターフェース経由**でデータに触る。
`LocalRepository` と `SheetRepository` を差し替えるだけで保存先が変わること。

設定もタスクと同じ層に置く。保存先を差し替えたときに設定だけ端末に取り残されないため。

```ts
interface Repository {
  // ...上記に加えて
  loadSettings(): Promise<Settings>;
  saveSettings(settings: Settings): Promise<void>;
  replaceAll(tasks: Task[]): Promise<void>;  // JSONバックアップの取り込み用
}
```

### 5.4 Settings（勤務時間）

```ts
interface WorkHours {
  start: string;       // 始業 "HH:mm"
  breakStart: string;  // 昼休憩の開始
  breakEnd: string;    // 昼休憩の終了
  end: string;         // 終業 "HH:mm"
  workDays: number[];  // 稼働曜日。0=日 〜 6=土
}

interface Settings {
  workHours: WorkHours;
  defaultEstimateMin: number;  // 未見積タスクを積むときの既定値（分）
  parseEndpoint: string;       // AI構造化プロキシのURL。空ならローカル解析
  voiceEnabled: boolean;
}
```

**既定値**（利用者の実際の勤務に合わせる。設定画面から変更可能）

| 項目 | 値 |
|---|---|
| 始業 | **8:20** |
| 昼休憩 | **12:25 〜 13:05** |
| 終業 | **17:10** |
| 実働 | 4時間05分 + 4時間05分 = **8時間10分**（490分） |
| 稼働曜日 | 月〜金 |

この値は表示だけでなく計算に使う。

- スケジュールビューの目盛り（始業・昼休憩・終業の線）
- その日のタスク見込み合計 ÷ 実働490分 = **稼働率**
- 一覧トップと分析画面の稼働ゲージ
- 勤務時間そのものの ICS 書き出し

---

## 6. 機能仕様

### 6.1 入口（Ports In）

#### 6.1.1 音声入力

録音まわりは NoteLoop 9.2（アプリ版）と同じ仕組みにそろえる。違うのは出口だけで、
NoteLoop が議事録を作るところを、Taskport はタスク候補を作る。

- 画面下部の丸ボタンをタップで開始。録音中は全画面に切り替わる
- 上にウェーブと経過時間、その下に認識テキストをそのまま流す
- `continuous: true` / `interimResults: true` / `lang: "ja-JP"`。
  認識が勝手に切れたら確定分をコミットして張り直す
- 終了時に自動で AI 構造化へ渡し、**確認画面（ReviewSheet）を必ず通す**

**表示**は NoteLoop 9.2 の体裁にそろえる。並びは
経過時間 → ゲージ → 「文字起こし」の仕切り → 認識テキスト → 案内文 →
左:やめる／中央:停止／右:一時停止。

ゲージは固定位置の縦バーが音量で伸縮する形（本ごとに伸びやすさと
揺れの速さを変えてあり、静かになると丸い点に戻る）。

ただし**全画面ではなく画面下半分のポップアップ**として出す。
上半分に一覧が見えたまま録音できる。背景は暗くするだけで、
タップしても録音は止めない（うっかり触れて消えるのを防ぐ）。

**画面を消しても止まらないようにする**（NoteLoop と同じ3点）

| 仕組み | 目的 |
|---|---|
| 常駐通知（Service Worker） | 録音中の表示。通知から一時停止・録音完了ができる |
| ほぼ無音の音声をループ再生 | メディア再生中のタブは凍結されないため、画面オフでも録音が続く |
| Wake Lock（任意） | 画面を点けたままにする。設定で切り替え |

**録音の取りこぼし対策**（NoteLoop と同じ3段構え）

- 3秒ごとにデータを取り出し、常に手元に確定させる
- 4秒ごとに録音が生きているか見張り、止まっていたらマイクを取り直して継ぎ足す
- 停止時に「経過時間」と「音声の実長」を比べ、足りなければ画面で知らせる

**音声の保存**

録った音声は端末内（IndexedDB）にのみ置く。外部へ送るのは認識後のテキストだけ。
新しい方から20本まで残し、古いものは自動で消す。録音履歴から再生・保存・削除ができ、
「あのタスク、本当にそう言ったか」を後から確かめられる。設定で保存を切ることもできる。

エラー時の挙動:

- `not-allowed` → マイク許可の確認を促し、キーボード入力へ誘導
- `no-speech` → 認識は張り直し、録音は続ける
- API 未対応 → ボタンをキーボード入力に切り替え

#### 6.1.2 キーボード入力（自然文）

音声と**まったく同じ**構造化パイプラインを通す。PCでの主入力手段。
メールの文面をそのまま貼り付けてタスク化する用途も想定する。

#### 6.1.3 フォーム直接入力

AIを経由せず1件を直接作る。件名と期限だけ分かっている時に最短で入れるための経路。

#### 6.1.4 他アプリからの共有（Web Share Target）

`manifest.webmanifest` に `share_target` を宣言し、Android の共有メニューに Taskport を出す。
Gmail・LINE・ブラウザから文面を共有すると、その本文が構造化パイプラインに入る。

```json
"share_target": {
  "action": "/taskport/share",
  "method": "GET",
  "params": { "title": "title", "text": "text", "url": "url" }
}
```

**これが「いろんな方法で入力」の中核**。メールで来た依頼をタスク化する経路が、
アプリを開き直さずに成立する。

#### 6.1.5 AI構造化パイプライン（共通）

すべての自然文入力は `parseToTasks(text)` に集約される。

- 入力: 自然文、今日の日付
- 出力: `Draft[]`
- 挙動:
  - 複数の用件を含む文は複数タスクに分割
  - 「明日」「来週月曜」「今月末」を実日付に変換
  - 期限の言及がなければ `due = null`
  - 「至急」「今日中」→ high、数日以内の期限あり → mid、それ以外 → low
  - 相手先・数量・背景は `note` へ

**必ず確認画面（ReviewSheet）を経由し、無確認で登録しない。**

### 6.2 出口（Ports Out）

#### 6.2.1 一覧ビュー

タブ: **今日 / 今週 / すべて / 完了**

- 今日 = 期限が今日以前の未完了（超過分を含む）
- 今週 = 期限が7日以内の未完了
- 並び順: 期限昇順 → 優先度降順
- 期限超過は日数を赤字で表示（「3日超過」）
- 各カードの左端に優先度の色帯
- 手順（サブタスク）を持つカードは「2/4」と進み具合の帯を出し、開くとその場でチェックできる

**検索と絞り込み**（タブの上）

- 件名・メモ・区分・手順を横断。空白区切りは AND、NFKC で全角半角と大文字小文字の揺れを吸収
- 区分（大分類）／優先度／期限（超過・今日・7日以内・それ以降・期限なし）／完了を含めるか
- 絞り込み中はタブを離れ、台帳の全件から探す
- よく使う条件は名前を付けて残せる（端末内にのみ保存）

#### 6.2.1b 繰り返しと手順

- **繰り返し** … 毎日／稼働日ごと／毎週（曜日を選ぶ）／毎月おなじ日／毎月末。
  完了にした時点で次の1件だけを作る（§10.1）。「いつまで」を過ぎたら作らない。
  自然文からも拾う（「毎週月曜に…」「毎営業日に…」「毎月末に…」）
- **手順（サブタスク）** … 1階層。全部済にしてもタスクは完了にならない。
  繰り返しの次回ぶんには、チェックを外した状態で引き継ぐ

#### 6.2.1c 期限のリマインド

時刻を入れたタスクの N 分前（既定10分）に通知する。期限が日付だけのものは対象外。

- 予約つき通知が使える端末 … 7日先までを Service Worker に預ける。閉じていても出る
- 使えない端末 … アプリを開いている間だけタイマーで出す

どちらも取りこぼしがあるため、いまどちらで動いているかと出ない条件を設定画面に書く。
既定は切。プッシュサーバは持たないので、予定が外部へ出る経路はない。

#### 6.2.2 スケジュールビュー

日付を縦軸にした2週間分の表示。期限なしタスクは末尾に「期限未設定」として集約。
「いつ何が固まっているか」を面で把握するための出口。

#### 6.2.2b 分析ビュー

処理の傾向を面で見るための出口。グラフの作りは同じ作者の StudyDeck（成績画面）に合わせる。

| カード | 内容 |
|---|---|
| 本日の稼働 | 勤務時間に対する積み上がり（%）＋連続処理日数＋未完了/超過/今週/完了 |
| 処理の推移 | 日別の完了件数（棒）と消化率（折れ線）を1つの図に。表示日数はスライダー |
| 区分ごとの時間 | 大分類ごとの時間と構成比。日報の集計と同じ見方。**完了したタスクの見込み時間の集計**であり実績ではない旨をカードに明記する |
| 区分ごとの進み具合 | 区分別の完了率を横バーで |
| 未完了の優先度分布 | 高/中/低の残件数を縦バーで |
| どの入口から入ったか | `source` 別の内訳。育てる入口を判断するために置く |

#### 6.2.3 Googleカレンダー

**書き出し（認証不要）**

- 単発: カレンダー登録URLを生成して開く
- 一括: 選択したタスクを `.ics` として書き出し、カレンダーにインポート
- 時刻なしのタスクは終日予定として扱う
- 勤務時間（始業・昼休憩・終業）そのものを ICS にできる

**連携（要認証）**

利用者が自分の Google Cloud で OAuth クライアントIDを作り、設定画面に入れて使う。
アプリにIDを埋め込まない。

| 向き | 内容 |
|---|---|
| 読込 | 2週間分の予定を読み、スケジュールの帯の右側に破線で重ねる。台帳には混ぜない |
| 取込 | 予定をタップするとタスク候補になる。**確認画面を通してから**登録する |
| 追加 | 選んだタスクを予定として追加する。押したときに選んだ分だけを送る |

トークンはメモリにだけ置き、localStorage には保存しない。

#### 6.2.4 テキスト書き出し

日報・朝会用に整形したプレーンテキストをクリップボードへ。

```
【8/18（火）実施】
・サンプル商事に AB-1234 の注残8000本の前倒し可否を確認 → 完了
・パレット稟議書を仕上げる

【翌日以降】
・8/20 サンプル物流 田中様へ CD-5678 の納期回答
```

#### 6.2.4b 業務日報の書き出し

資材課の業務日報（エクセル）へ貼り付けるための表。勤務時間を 30 分で刻んだ
16 コマに、その日のタスクを時刻順に置く。区切りはタブ（区分名に読点が入るため
CSV では割れる）。30 分を超えるタスクはコマ数ぶんに広げ、時刻の無いタスクは
空いているコマへ順に置く。

```
8:20～8:50	納期確認、日程調整	サンプル商事へ AB-1234 の納期を確認する
8:50～9:20	発注業務、所要量確認	CD-5678 を200個発注する
```

#### 6.2.5 CSV書き出し

全項目をCSVで出力。Excelでの集計や、上長への共有の逃げ道として必ず用意する。

### 6.3 画面一覧

| 画面 | 役割 |
|---|---|
| Home | 一覧＋下部入力ドック。起動時の既定画面 |
| ReviewSheet | AI候補の確認・修正・件単位の破棄 |
| TaskEditor | 既存タスクの編集 |
| ScheduleView | 日付軸表示。勤務時間の帯に予定を置く |
| DashboardView | 分析。稼働率・処理の推移・区分別・優先度分布・入口別 |
| RecordingOverlay | 録音中のポップアップ（画面下半分）。経過時間・ゲージ・認識テキスト |
| RecordingsView | 録音履歴。再生・音声の保存・削除 |
| ExportSheet | 出力形式の選択 |
| Settings | 表示設定、データ書き出し／取り込み |

---

## 7. デザイン方針

### 7.1 トークン

初版は白地の伝票調で設計していたが、NoteLoop 9.2 に合わせてダークテーマへ変更した（§10.1）。
実装値は `src/styles/tokens.css` を正とする。

```css
:root{
  /* 面と文字 — マットなグレーがかった黒。層は明度差ではなく落ち影で作る */
  --bg:      #17181b;
  --surface: #212328;
  --ink:     #e7e9ee;
  --muted:   #a3a9b5;
  --line:    #2e3138;
  /* ブランド（入口＝Ports In の色） */
  --brand1:  #7b93ff;
  --brand2:  #a68cff;
  --on-brand:#17181b;  /* 明るいブランド面の上に載せる墨 */
  /* 優先度 — 初版の色相を保ち、暗い面で読める明度に上げた */
  --pri-high:#ff7a6b;  /* 高（元 #B03A28） */
  --pri-mid: #e2a33f;  /* 中（元 #B87A22） */
  --pri-low: #78aed6;  /* 低（元 #3F7194） */
  --go:      #4ade80;  /* 完了・確定操作 */
}
```

### 7.2 タイポグラフィ

- 本文: システムの日本語ゴシック（`-apple-system` → `Hiragino Kaku Gothic ProN` → `Noto Sans JP` → `Yu Gothic UI`）。Webフォントは読み込まない（通信環境の悪い倉庫内で確実に表示させるため）
- **日付・優先度・件数は等幅（`ui-monospace`）＋ `tabular-nums`**。桁が揃うことで一覧の走査が速くなる。生産管理の伝票の質感を意図している
- 小ラベルは 10px・`letter-spacing: .14em` の大文字。装飾ではなく、情報の種別を示す標識として使う

### 7.3 レイアウト原則

- **入力ドックは常に画面下部固定**。片手持ちの親指到達域に録音ボタンを置く
- カードの角丸は 3px に抑える。丸すぎるとタスクが「柔らかいもの」に見え、期限の緊張感が消える
- 優先度は色帯（左端5px）で示し、テキストに色を付けすぎない

### 7.4 品質の下限（明示的に満たすこと）

- 320px 幅まで崩れない
- 文字と背景のコントラストは WCAG AA（本文 4.5:1）以上
- キーボードのみで全操作が可能、フォーカスリングを消さない
- `prefers-reduced-motion: reduce` でパルスアニメーションを停止
- タップ領域は最小 44×44px

---

## 8. セキュリティとデータの扱い

業務情報（取引先名・品番・数量・社内の人名）を扱う前提で、以下を厳守する。

| 項目 | 方針 |
|---|---|
| 音声データ | 端末内で処理し、外部送信しない |
| 自然文の解析 | 端末内で完結。外部のAIサービスへは送らない |
| タスクデータ | 端末内 IndexedDB のみ。**例外は Googleカレンダーへの追加だけ**で、利用者が選んで押したときにその分のみ送る（§10.1） |
| 録音した音声 | 端末内 IndexedDB のみ。外部へ送信しない。20本まで。設定で保存を切れる |
| Googleのトークン | メモリのみ。localStorage に保存しない |
| リポジトリ | GitHub Pages 用に Public。**実データ・スクリーンショット・取引先名を含むサンプルをコミットしない** |
| サンプルデータ | 架空の会社名・品番を使う |
| バックアップ | Settings からの JSON 書き出し／取り込みで対応（端末故障・キャッシュ削除への備え） |

**リポジトリを Public にする以上、コミット前に取引先名が混入していないか必ず確認すること。**
Private リポジトリ＋Cloudflare Pages に切り替える選択肢も残す。

---

## 9. 段階計画

### Phase 0 — 試作（完了）

React 単一ファイルで、音声入力 → AI構造化 → 確認 → 一覧 の一連の流れを検証済み。

### Phase 1 — 自分専用 PWA

**目標**: 毎日実際に使える状態にする。

- [x] リポジトリ作成、Vite + React + TS の初期構築
- [x] `types.ts` と `Repository` インターフェースの確定
- [x] `LocalRepository`（IndexedDB）実装
- [x] 音声入力・キーボード入力・フォーム入力の3経路
- [x] ReviewSheet（確認・修正）
- [x] 一覧ビュー（今日／今週／すべて／完了）
- [x] PWA 化、GitHub Pages へのデプロイ設定（Android ホーム画面への追加は実機で確認）
- [x] JSON バックアップ書き出し／取り込み

**完了条件**: 1週間、紙のメモを使わずに運用できること。

### Phase 2 — 出口を増やす

- [x] スケジュールビュー（勤務時間のタイムライン＋2週間の日付軸）
- [x] Web Share Target（他アプリからの共有）
- [x] Googleカレンダー書き出し（URL / ICS）
- [x] 日報用テキスト書き出し
- [x] CSV書き出し
- [x] 分析ビュー（§6.2.2b）

### Phase 3 — 共有

- [ ] `SheetRepository` 実装（GAS + Googleスプレッドシート）
- [ ] 担当者フィールドの追加
- [ ] 差分同期と競合解決（最終更新優先）
- [ ] 共有範囲の設計（誰のタスクが誰に見えるか）

**方針**: 社内は Google Workspace のため、共有先は GAS + スプレッドシートとする。
管理職がスプレッドシートを直接開けることを利点として活かす。

---

## 10. 判断の記録

後から「なぜこうしたか」を思い出せるように、迷った点と結論を残す。

| 論点 | 結論 | 理由 |
|---|---|---|
| ネイティブ vs PWA | PWA | 1コードベースで Android と PC を賄える。ストア審査が不要で、直したその日に反映できる |
| 音声認識をAPI（Whisper等）にするか | ブラウザ内蔵 | 無料、遅延が小さい、そして何より音声データを外に出さずに済む。iOS 対応が必要になった時点で再検討 |
| AI構造化を全自動にするか | 確認を挟む | 期限の誤解釈が黙って登録される損失が大きい。ユーザーの明示的な希望でもある |
| 保存先を最初からクラウドに | しない | 個人利用の段階でサーバを持つと運用が重い。インターフェースだけ切っておけば後から足せる |
| 名前に Voice を入れるか | 入れない | 音声は入口の一つに過ぎない。多入力・多出力という本質を名前に持たせる |

### 10.1 実装時に決めたこと（v1.0 実装で追記）

| 論点 | 結論 | 理由 |
|---|---|---|
| 見た目を §7 の伝票調のままにするか | **NoteLoop 寄りに変更** | 利用者の指示。同じ作者の NoteLoop と操作感を揃えると、2つのアプリを行き来しても迷わない。等幅・色帯・角丸の抑制（§7.2〜7.3）は残し、面の作り（ヘッダー、ガラス面のパネル、下部固定のマイク）を NoteLoop に合わせた |
| どの版の NoteLoop に合わせるか | **9.2（現行）のダークテーマ** | 初回は既定ブランチ（`claude/app-creation-5z8ppb`）の明るいグラデーション版を見て作ってしまった。実際に配信されているのは `main` の 9.2 で、マットなグレーがかった黒がベース。利用者の指示で現行に合わせ直した。以後 NoteLoop を参照するときは **`main` ブランチ**を見ること |
| 優先度の色を §7.1 のまま使うか | **色相を保ったまま明度を上げる** | §7.1 の値（#B03A28 / #B87A22 / #3F7194）は白地の伝票を想定したもので、#17181b の暗い面ではコントラストが足りない。色相は保ちつつ #ff7a6b / #e2a33f / #78aed6 に上げ、WCAG AA（4.5:1）を満たすことを確認した |
| 明るいブランド面の上の文字色 | **墨（#17181b）を載せる** | ダーク版のブランド色（#7b93ff / #a68cff）は明度が高く、白文字だとコントラストが取れない。ボタン・タブ・チェックなどブランド色で塗る面はすべて墨文字にした |
| グラフの作り | **StudyDeck の成績画面に合わせる** | 利用者の指示。棒＋折れ線を1図にまとめ、目標線を1本引く形は既に使い慣れている。ゼロから別の見せ方を作る理由がない |
| `Task.estimateMin` の追加 | **追加する** | 勤務時間（8:20〜17:10）を設定した以上、「その日に収まるか」を出せないと設定が飾りになる。null 許容にし、未見積は `defaultEstimateMin` で埋めるので既存データは壊れない |
| 勤務時間を Task 側に持つか | **Settings に持つ** | 勤務時間はタスクの属性ではなく利用者の属性。タスクごとに持たせると全件更新が必要になる |
| AIプロキシ未設定時の挙動 | **端末内のかんたん解析にフォールバック** | プロキシを立てる前でもアプリが使えないと、Phase 1 の完了条件（1週間、紙のメモを使わずに運用）に届かない。フォールバック経路も必ず ReviewSheet を通すので、原則1（AIは提案し、人が決める）は崩れない。どちらの経路で解釈したかは確認画面に明示する |
| 音声認識の実装 | **NoteLoop の作りをそのまま踏襲** | `continuous` + `interimResults` で、認識が勝手に切れたら確定分をコミットして張り直す。この挙動は NoteLoop で実地検証済みで、作り直す理由がない |
| 分析ビューの追加（§6.3 の画面一覧に追加） | **追加する** | 利用者の指示。`source` を記録する設計上の意図（どの入口を育てるか）が、見る場所がないと活きない |
| 公開パスの大小 | **`/Taskport/`（リポジトリ名に合わせる）** | design.md 初版は `/taskport/` を想定していたが、実際のリポジトリ名は `Taskport`。GitHub Pages のパスがリポジトリ名の大小を区別する場合に全アセットが 404 になるため、想定URLの方をリポジトリ名に合わせた |
| 昼休憩の既定値 | **12:25〜13:05（実働 8時間10分）** | 初回は 12:20 開始で作ったが、実際の勤務は 12:25 開始。利用者の指摘で修正した |
| 録音の作りをどこまで NoteLoop に合わせるか | **画面・通知・音声保存まで合わせる** | 利用者の指示。同じ端末で2つのアプリを使うので、録音中の見え方と操作が違うと迷う。NoteLoop 9.2 で実地検証済みの仕組み（3秒ごとのデータ確保・見張りと録り直し・無音再生によるバックグラウンド維持）をそのまま持ってきた。出口だけ差し替え、議事録ではなくタスク候補を作る |
| 録音した音声を残すか | **既定で残す（20本まで）** | 「登録されたタスクが言ったことと違う」ときに元を確かめられないと直しようがない。端末内にのみ置き、外部へは出さない。容量を食うので本数を絞り、設定で切れるようにした |
| Googleカレンダーへタスクを送るか | **送る（利用者が選んだ分だけ）** | 利用者の指示。§8 の「タスクデータをサーバに置かない」と衝突するため、**自動送信はしない**・押したときに選んだ分だけ送る・画面に「Google に渡ります」と明示する、の3点で限定した。読込側は台帳に混ぜず、取り込みも確認画面を通す |
| クライアントIDをアプリに埋め込むか | **埋め込まない** | 公開リポジトリなので、利用者が自分の Google Cloud で作ったIDを設定画面に入れる方式にした。承認済みの生成元で縛られるため、IDが漏れても他所からは使えない |
| 外部AIで自然文を解析するか | **やめる（端末内の解析のみ）** | 利用者の指示。取引先名・品番・数量を含む文章が端末から出なくなり、鍵の管理もプロキシの運用も要らなくなる。代わりに期限や優先度の読み取りは素朴になるため、確認画面での確認がこれまで以上に要になる。Cloudflare Workers のプロキシは削除した |
| IndexedDB の版を上げるか | **上げない（保存先を分ける）** | v1.2.0 で録音を足す際に版を1→2へ上げたところ、別のタブが古い版を掴んでいるとアップグレードが永久に待たされ、「読み込んでいます…」から進めなくなった。版を固定するのをやめ、台帳は版を指定せず開き、録音は別DB（`taskport-media`）に分けた。以後、保存先が増えてもDBを足すだけで台帳には触らない |
| 録音中を全画面にするか | **画面下半分のポップアップ** | 利用者の指示。全画面だと録音中に一覧が見えず、「いま何が残っているか」を確かめながら喋れない。見た目は NoteLoop 9.2 のまま（経過時間・縦バーのゲージ・3つのボタン）にして、出し方だけ変えた |
| 昼休憩のほかに小休憩を持つか | **持つ（既定 10:20〜10:25 / 15:05〜15:10）** | 利用者の日報の様式から判明した。始業〜終業から昼休憩だけを引くと 490 分になるが、日報の枠は 16 コマ×30 分＝**480 分**で組まれている。差の 10 分は午前と午後の 5 分休憩。稼働率・空き時間・タイムラインのすべてがこの値に依存するので、`WorkHours.shortBreaks` を足して実働を 8 時間ちょうどに合わせた。設定画面で増減できる |
| 区分をマスタ管理するか | **する（§5.1 の判断を差し替え）** | §5.1 は「自由入力とし、マスタ管理はしない。実運用で頻出する語が固まってから候補化する」としていたが、日報の様式として既に固まっていた。その語をそのまま候補にする（8大分類・35小分類、`src/lib/workCategories.ts`）。自由入力は残し、マスタ外の語は「その他」として集計する |
| 自然文から区分を当てるか | **語のみで当てる（外れたら空欄）** | 「発注」「棚卸」「見積」のように区分がほぼ一意に決まる語だけを表に持ち、当たらなければ空欄のまま確認画面に出す。推測で埋めると誤った区分が黙って集計に入るので、当てないほうを既定にした |
| 分析の「区分ごとの時間」を実績とするか | **見込み時間の集計であると画面に書く** | 台帳が持っているのは見積（`estimateMin`）であって実際にかかった時間ではない。日報の集計（0.25 時間×コマ数）と同じ見え方をするため、そのまま出すと実績と誤読される。カード内に「完了したタスクの見込み時間の集計で、実際にかかった時間ではありません」と明示した |
| 業務日報の書き出し形式 | **タブ区切り（TSV）** | エクセルへ貼るのが唯一の用途。CSV だと区分名に含まれる読点がセルを割ってしまう。時刻の枠は日報と同じ 16 コマで、30 分を超えるタスクはコマ数ぶんに広げ、時刻の無いタスクは空いた枠へ順に置く |
| 一覧の探し方をタブだけにするか | **検索と絞り込みを別に置く** | タブ（今日/今週/すべて/完了）は「いつやるか」で切る道具で、「あの件どうなったか」を探すのには向かない。台帳が数百件になると届かなくなるので、件名・メモ・区分・手順を横断する検索と、区分（大分類）・優先度・期限での絞り込みを足した。**絞り込み中はタブを離れて全件から探す**（「今日」に立ったまま来月の1件を探して0件、という迷い方を防ぐ）。よく使う条件は名前を付けて残せる（`Settings.savedFilters`） |
| 繰り返しの単位をどこまで持つか | **5つだけ（毎日／稼働日ごと／毎週／毎月おなじ日／毎月末）** | 「2週おき」のような間隔は持たせない。日報・週次会議・棚卸・月初集計という実際の定例がこの5つで足りる。選択肢が増えるほど確認画面が重くなる |
| 繰り返しの次回をいつ作るか | **完了にした時点で1件だけ** | 先の分をまとめて作ると、一覧が未来のタスクで埋まって「いま何をやるか」が見えなくなり、予定が変わったときに作り置いた分を全部直すことになる。1件ずつなら、やめたければその1件を消せば終わる。遅れて完了したときは過ぎた回を飛ばし、前倒しで完了したときはその回を消費して次へ進む |
| サブタスクを何階層にするか | **1階層（手順の中に手順は作らない）** | 階層が増えると、いま何をやるかを探す手間のほうが大きくなる。カードから開いてその場でチェックできるようにし、1つ潰すたびに編集画面へ入らずに済ませた。手順を全部済にしてもタスクは完了にしない（完了にするのは人の操作） |
| リマインドをどう配るか | **端末内だけ。2つの道を実行時に選ぶ** | プッシュサーバを持つと §8 の「タスクデータをサーバに置かない」が崩れる。予約つき通知（Notification Triggers）が使える端末では7日先までを Service Worker に預け、使えない端末ではアプリを開いている間のタイマーで出す。**どちらも取りこぼしがある**ので、いまどちらで動いているかと出ない条件を設定画面にそのまま書く。既定は切 |
| 保存データを開くのを時間で打ち切るか | **打ち切らない（v1.6.1 で変更）** | v1.2.1〜1.6.0 は「8秒で開かなければ失敗」にしていた。ところが実機（Android・ホーム画面から起動）では、健全なDBでも起動直後の混み合いで8秒を超えることがあり、しかも諦めるときに開きかけの接続をこちらから閉じていたため、あと少しで開くはずのものを自分で壊していた。**待ちは打ち切らず、時間がかかっていることだけを画面に伝える**方式に変えた。開けた時点で画面が直る |
| 保存データが開けないとき、画面をどうするか | **画面は動かし、上に理由を出す** | 1つ開けないだけで全部の画面が使えなくなると、書き出しにも設定にも手が届かなくなる（v1.6.0 の実機がこの状態）。台帳は空のまま画面を出し、「いまタスクは読めておらず、新しい登録も保存できない」ことと、ブラウザが返した素の理由・空き容量を並べる。最後の手段として「保存データを作り直す」を置いた（消えることを先に伝えてから実行する） |
| 地の色を黒にするか白にするか | **白（v1.7.0 で変更）** | 利用者の指示。「黒背景が見にくい」。現場と事務所を行き来する使い方では、明るい場所で暗い面はまぶしさに負けて読めなくなる。面は白〜ごく薄いグレー、文字は墨にした。**ヘッダーと引き出しだけは色面（藍〜菫）のまま**にしてある。画面の上端に色があると Android のステータスバーと継ぎ目なく繋がり、どのアプリを開いているか一目で分かるため。優先度の色は §7.1 の値（白い伝票を想定した色）に戻した。ただし §7.1 の中 `#b87a22` は白地で 3.6:1 しかないので `#8a5a12` に落とし、全色で WCAG AA（4.5:1）を満たすことを確かめた |
| Googleカレンダーの設定をどこまで案内するか | **コンソールへ直接飛べる4手順にする** | Google Cloud の画面は階層が深く、「APIを有効化 → 同意画面 → クライアントID」の順にたどるのが毎回手間になる。設定画面に手順を並べ、それぞれのコンソールへのリンクと、承認済み生成元をそのまま写せるコピーボタンを置いた。リンクを押した時点では何も送らない |
| タスクをサーバに置くか（§8 の見直し） | **置く（Google Drive のアプリ専用フォルダ・既定は切）** | 利用者の指示「スマホとPCで同時に追加・完了させたい」。§8 の「タスクデータをサーバに置かない」を、ここで初めて崩す。置き場は Drive の `appDataFolder`（マイドライブに出てこず、ほかのアプリからも Drive の画面からも見えない）。**タスクの件名・メモ・区分が Google に保存される**ので、既定は切、設定で入れたときだけ動く、設定画面にそのまま書く、の3点で限定した。音声は今までどおり外へ出さない |
| 同期の置き場をどれにするか | **Drive（GAS＋スプレッドシートではなく）** | design.md の Phase 3 は GAS＋スプレッドシートを想定していたが、Drive なら**カレンダー用に作ったクライアントIDをそのまま使い、スコープを1つ足すだけ**で済み、デプロイするものが無い。中身を目で見たいという要望が出たら、書き出し（JSON/CSV）でも足りる |
| 同時に触ったときの決着 | **1件ずつ、更新時刻の新しいほう（墓標つき）** | ファイル全体を上書きすると片方の操作が丸ごと消える。ID ごとに `updatedAt` を見て新しいほうを採り、削除は墓標（ID→消した時刻）で伝える。**同じ1件をほぼ同時に直すと、あとから保存したほうだけが残る**（項目ごとには混ぜない）。一人で2台という前提での割り切りで、設定画面にも書いた。墓標は90日で掃除する |
| 設定も同期するか | **しない（タスクだけ）** | 勤務時間や保存した絞り込みは端末ごとの都合が入る（PCと現場で違う枠を使うこともある）。混ぜると衝突の解決が要る割に得が小さい。設定の持ち運びは JSON バックアップで足りる |
| タスクを時間帯に割り当てるか | **割り当てる（タイムボックス）** | 一覧として持つだけでは「その日に本当に入るか」が全部やってみるまで分からない。枠ごとに見積もりの合計と残りを出し、詰め込みすぎを朝の時点で気づけるようにした。枠は**勤務時間の区切りそのもの**（＝日報の時間区分）。独自の時間割を作らないのは、日報へ書き写すときにずれるため。粗さは「帯」まで（分刻みにすると少し狂っただけで全部組み直しになる） |
| 次にやる1件を選ぶ手間 | **アプリ側が1件だけ出す** | いまの枠の先頭を1件だけ大きく出し、終わると次に入れ替わる。何をやるか毎回選び直す時間をなくす（シングルタスク） |
| 溜まった超過をどう片づけるか | **朝の仕分け（1件ずつ4択）** | 一覧の中で1件ずつ開いて直すのは手間がかかり、結局そのまま溜まる。今日やる（枠も選ぶ）／明日へ／期限を外す／もう終わっている の4つに絞った。**開いた時点の一覧を写し取って**回す（さばくたびに「超過」でなくなって列が縮み、次を飛ばすため） |
| 録音ゲージを何で動かすか | **マイクの実音量（RMS）**（v1.9.0 で変更） | v1.8.0 まで「音声認識が結果を返したら 0.85 にする」だけで動かしていたため、認識が返ってこない場面（通信が細い・非対応・喋り始めた直後）ではゲージが止まって見えた。録音に使っている流れをそのまま分岐させて実測に変えた。録音を残さない設定でもゲージのためだけにマイクを取り、止めるときに必ず手放す。音は測るだけで外へ出さない |
| 勤務日を曜日だけで決めるか | **会社カレンダーを実日付で持つ** | 祝日・一斉有給・年末年始は平日でも休みで、逆に土曜出勤の日もある。曜日の設定だけでは稼働率も空き時間も合わない。会社が配るカレンダーがその答えなので、休日と出勤日を実日付で持ち、曜日より優先する。判定は「出勤の指定 → 休みの指定 → 曜日」の順（休みの週に1日だけ出る、を表せるようにするため） |
| 会社カレンダーの入れ方 | **Googleカレンダーからの取り込みと、月の枠の手入力の両方** | 会社のカレンダーを Googleカレンダーに追加してあるなら取り込みが速い（終日予定だけを見て、件名から見当づける。祝日の名前も拾う。当てられないものは人が選ぶ）。紙で配られただけなら、月の枠を押して1日ずつ決める。年間の稼働日数と休日数を出しているのは、紙の数字と突き合わせて入れ間違いを自分で確かめられるようにするため |
| 会社カレンダーを同期に乗せるか | **乗せる（設定の中でこれだけ）** | 両方の端末で違う勤務日を使うと、稼働率も空き時間も食い違う。丸ごと更新時刻の新しいほうを採る（1日ずつは混ぜない）。勤務時間や保存した絞り込みは端末ごとの都合が入るので、今までどおり同期しない |
| 画面を端末ごとに変えるか | **変える（1024px と 1180px で切り替え）** | 利用者の指示と、参考にした画面（Smartsheet のダッシュボード）。**PC** は左にナビを出しっぱなしにし、一覧を「左＝その日の状況／右＝探して片づける」の2カラムに、分析はカードを2〜3枚ずつ並べる。文字入力欄は最初から開けておき、キー操作（`/` 探す・`n` 直接入力・`e` 書き出し・`1〜4` タブ・`Esc` 閉じる）を足した。**スマホ** は1カラムのまま、検索とタブをヘッダーの下に貼り付けて、一覧が長くなっても手が届くようにした。端末の見分けは幅だけでなく「細かく指せる装置があるか」も見る（タブレットは広くても指で触るため） |
| ID生成に ULID ライブラリを入れるか | **入れない（26行の自前実装）** | 依存は最小に保つ（CLAUDE.md §2）。Crockford Base32 の時刻+乱数は自前で十分短く書ける |

---

## 11. 用語

| 語 | 意味 |
|---|---|
| Port In | 入口。タスクがシステムに入ってくる経路 |
| Port Out | 出口。タスクが別の形で取り出される経路 |
| Draft | AIが提案した未保存のタスク候補 |
| 構造化パイプライン | 自然文を Draft 配列に変換する共通処理 |
