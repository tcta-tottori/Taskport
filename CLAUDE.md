# CLAUDE.md — Taskport 作業規約

このファイルは Claude Code がこのリポジトリで作業する際の指針です。
実装を始める前に必ず `design.md` を読んでください。

---

## 1. このプロジェクトは何か

**Taskport** — タスクとスケジュールを、複数の端末から複数の方法で入力し、
複数の形式で出力するための個人用ハブ（PWA）。

利用者は製造業（生産管理部）の実務者1名。Android スマートフォンを主、PC を副として使う。
将来的に部署内での共有を想定している。

**この設計で最も重要な構造**は、入口（Ports In）と出口（Ports Out）が
中央のタスク台帳を介して疎結合であること。入口も出口も必ず増える前提で書く。

---

## 2. 技術スタック

| 項目 | 内容 |
|---|---|
| ビルド | Vite |
| 言語 | TypeScript（`strict: true`） |
| UI | React 18（関数コンポーネント + Hooks のみ） |
| スタイル | プレーンCSS + CSS変数。CSS-in-JS やUIフレームワークは導入しない |
| 保存 | IndexedDB（`idb`） |
| PWA | vite-plugin-pwa |
| 配信 | GitHub Pages |

**依存は最小に保つ**。新しいライブラリを入れる前に、標準APIで書けないか検討し、
入れる場合は理由をコミットメッセージに書く。

---

## 3. 絶対に守ること

### 3.1 セキュリティ

- **外部のAIサービスを呼ばない。** 自然文の解析は端末内で完結させる（design.md §10.1）
- **実データをコミットしない。** テストデータ・サンプル・スクリーンショットに、実在の取引先名・人名・品番・数量を含めない。架空のもの（例: 「サンプル商事」「AB-1234」）を使う
  - **唯一の例外: 会社カレンダーの休日**（`src/lib/calendarPresets.ts`）。利用者の明示の指示で入れてある。
    ここでも**会社名・部署名は書かない**。日付だけを持ち、どこの会社のものか分からない形を崩さない。
    範囲を広げるとき（2027年ぶんなど）も同じ扱いにする。この例外を他のデータへ広げない
- リポジトリは Public。コミット前に上記の混入を確認する

### 3.2 データの扱い

- 音声データを外部に送信する実装を書かない。Web Speech API の認識結果テキストのみを扱う。
  録音した音声を端末内（IndexedDB）に置くのは可。外へ出す経路を作らない
- タスクデータが外へ出る経路は次の2つだけ。増やすときは必ず確認する
  - **Googleカレンダーへの追加**（利用者が選んで押した分だけ）
  - **端末どうしの同期**（Google Drive のアプリ専用フォルダ。既定は切）
  どちらも「自動で勝手に送らない・画面に明示する」を崩さない（design.md §10.1）
- GoogleのクライアントIDやトークンをコードに書かない。IDは設定画面から、トークンはメモリのみ

### 3.3 設計の骨格

- **画面コンポーネントから IndexedDB を直接叩かない。** 必ず `Repository` インターフェース経由
- **すべての自然文入力は `parseToTasks()` に集約する。** 音声用・テキスト用に別々の解析処理を書かない
- **AI が生成したタスクを確認画面なしで保存しない。** これは仕様であり、簡略化してはいけない
- 出力処理（`ports/out/`）は `Task[]` を受け取る純関数にする。React に依存させない

---

## 4. コーディング規約

### 4.1 全般

- 関数コンポーネントと Hooks のみ。クラスコンポーネントは使わない
- 型は `src/types.ts` に集約。各ファイルで独自にタスク型を定義しない
- `any` を使わない。外部APIのレスポンスは `unknown` で受けて絞り込む
- 日付は文字列 `"YYYY-MM-DD"` で保持する。Date オブジェクトをそのまま保存しない（タイムゾーンの事故を避ける）
- IDは ULID（時系列ソート可能）を使う

### 4.2 命名

- ファイル: コンポーネントは PascalCase（`TaskCard.tsx`）、それ以外は camelCase（`parseToTasks.ts`）
- 入口は `ports/in/`、出口は `ports/out/` に置く。ここに置かれていないものは入出力ではない
- CSSクラスは `tp-` 接頭辞（`tp-card`, `tp-dock`）

### 4.3 UI文言

- **日本語。敬体ではなく簡潔な常体寄り**（「登録しました」「保存に失敗しました」）
- ボタンは動作をそのまま書く（「2件を登録」「やめる」）。「送信」「OK」のような抽象語を使わない
- エラーは何が起きて次に何をすればよいかを書く。謝らない。曖昧にしない
  - 良い例: 「マイクが使えませんでした。ブラウザのマイク許可を確認するか、キーボード入力をお使いください。」
  - 悪い例: 「エラーが発生しました。」
- 空の画面は次の行動への案内にする。「データがありません」で終わらせない

### 4.4 スタイル

- 色は必ず `src/styles/tokens.css` の CSS変数から取る。ハードコードした16進数を書かない
- **地は白**（v1.7.0 で黒地から変更。design.md §10.1）。ヘッダーと引き出しだけ色面で、そこの文字は `--on-chrome`
- 面の作りは NoteLoop に合わせる。**参照するのは `main` ブランチ**（既定ブランチではない）。
  配色だけは Taskport 独自（白地）で、NoteLoop の暗い配色をそのまま持ち込まない
- 文字と背景のコントラストは WCAG AA（本文 4.5:1）以上
- 日付・件数・優先度は等幅フォント＋ `font-variant-numeric: tabular-nums`
- 320px 幅まで崩れないこと
- 端末で作り分ける。PC（1024px 以上かつ細かく指せる装置あり）は左ナビ常設・複数カラム・
  文字入力欄を開いたまま・キー操作あり。スマホは1カラムで、検索とタブをヘッダー下に貼り付ける
- フォーカスリングを消さない
- `prefers-reduced-motion: reduce` でアニメーションを止める
- タップ領域は最小 44×44px

---

## 5. 自然文解析の仕様

`src/ports/in/localParse.ts` は以下を守る。

- **端末内で完結させる。** 外部へ文章を送る経路を作らない
- 1文＝1タスクを基本とし、実行内容が読み取れない断片は捨てる
- 「明日」「来週月曜」「今月末」を、今日を基準に実日付へ変換する
- 期限の言及がなければ `due` は null。推測で日付を入れない
- 「至急」「今日中」は high、数日以内の期限があるものは mid、それ以外は low
- 区分は `src/lib/workCategories.ts` のマスタから語で当てる。当たらなければ空欄にする（推測で埋めない）
- 期限や優先度を表す語は件名から落とし、実行内容だけ残す
- 1件も取れなかったときは、入力そのものを1件の候補にする（捨てない）
- 繰り返しは「毎日」「毎週◯曜」「毎月末」のように明確な語だけ拾う。「定期的に」は拾わない

読み取りは素朴なので、**確認画面での確認が要になる**。
確認画面を省く実装を書かない。

## 6. 作業の進め方

### 6.1 段階を飛ばさない

`design.md` §9 のフェーズ順に進める。Phase 1 が動く前に Phase 3 の同期処理を書かない。

### 6.2 一度に一つ

大きな変更をまとめて出さない。1つの機能が動いてコミットできる状態にしてから次へ進む。

### 6.3 迷ったら聞く

以下は勝手に決めずに確認する。

- データモデルへのフィールド追加・削除
- 新しい依存ライブラリの導入
- `design.md` に書かれた仕様と異なる実装
- 保存先やホスティングの変更

### 6.4 設計書を更新する

実装の過程で設計判断が変わったら、`design.md` の §10「判断の記録」に追記する。
コードと設計書が食い違ったまま放置しない。

---

## 7. コマンド

```bash
npm install        # 依存インストール
npm run dev        # 開発サーバ
npm run build      # 本番ビルド（dist/）
npm run preview    # ビルド結果の確認
npm run lint       # 型チェックのみ
```

`vite.config.ts` の `base` は **リポジトリ名と完全に一致させる**こと（GitHub Pages のサブパス配信のため）。
現在のリポジトリ名は `Taskport` なので `base = '/Taskport/'`。
リポジトリ名を変えたら `base` も必ず合わせる。ここがずれると全アセットが 404 になる。

---

## 8. 動作確認の観点

機能を追加したら、以下を実機で確認してから完了とする。

- [ ] Android Chrome で動くか（PCのDevToolsだけで判断しない）
- [ ] 音声入力が失敗した時、キーボード入力に到達できるか
- [ ] 通信が切れた状態で一覧の閲覧・完了操作ができるか
- [ ] ホーム画面から起動した時にアドレスバーなしで開くか
- [ ] リロードしてもタスクが消えないか
- [ ] 320px 幅で崩れないか

---

## 9. やらないこと

- テストのために実際の業務データを使う
- 確認画面を省いて解析結果を直接保存する
- 外部のAIサービスへ文章を送る
- IndexedDB の版を上げる（保存先が増えるときは新しいDBを足す。design.md §10.1）
- 保存データを開くのを時間で打ち切る／開きかけの接続を閉じる（遅いだけの端末を壊す。design.md §10.1）
- 保存データが開けないときに画面全体を止める（書き出しと設定には手が届くようにする）
- 実働時間を数え直さずに勤務時間を触る（昼休憩のほかに小休憩がある。`src/lib/workday.ts` の `allBreaks` を通す）
- 「区分ごとの時間」を実績として見せる（あれは見込み時間の集計。カードの注記を消さない）
- 繰り返しの次回ぶんを先の分までまとめて作る（完了時に1件だけ。design.md §10.1）
- 手順（サブタスク）を全部済にしたらタスクを自動で完了にする（完了にするのは人の操作）
- 写真から読んだ休日を、確認の枠に出さずに保存する（読み違いが黙って稼働率を狂わせる）
- 写真やその中身を外へ送る／外部の文字認識にかける（`src/lib/calendarPhoto.ts` は色だけを端末内で見る）
- リマインドのために外部へ予定を送る（プッシュサーバは持たない。出ない条件は設定画面に明記する）
- 同期を既定で入れる／画面に断りなく上げる（切ってあることと、何が上がるかを設定画面に書く）
- 同期の衝突をファイル丸ごとの上書きで解決する（1件ずつ更新時刻で決める。design.md §10.1）
- タイムボックスの枠を勤務時間と別に作る（日報へ書き写すときにずれる）
- 稼働日を曜日だけで決める（会社カレンダーを通す。`isWorkDay(day, wh, cal)`）
- 会社名・部署名をコードに書く（休日の日付だけは §3.1 の例外として `calendarPresets.ts` に持つ。会社名は持たない）
- 録音ゲージを認識の合図だけで動かす（マイクの実音量を測る。`src/lib/micLevel.ts`）
- `Repository` を経由せずにデータへアクセスする
- UIフレームワーク（Material UI 等）の導入
- 過剰なアニメーション。動きは録音中のパルスと画面遷移に限る
