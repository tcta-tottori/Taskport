# AI 構造化プロキシ（Cloudflare Workers）のエンドポイント。
# 未設定でもアプリは動く（端末内のかんたん解析にフォールバックする）。
# 例: https://taskport-parse.<your-subdomain>.workers.dev/parse
VITE_PARSE_ENDPOINT=

# APIキーはここに書かない。Workers 側の環境変数（wrangler secret）に置く。
